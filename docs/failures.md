# failures（失敗の蓄積ログ / append-only・消さない）

同じ失敗を繰り返さないための**蓄積型**ログ。handoff（`docs/handoff.md`・毎回上書き）とは
役割が違い、ここは**消さずに積む**。1件＝**日付＋事象＋根因＋教訓**。

---

## 2026-08-11 Skillをインストール用パスだけに置き、リポジトリ内での直接利用条件を満たしていなかった

- **事象**：初回実装ではSkillを`skills/teaching-slide-package`へ配置した。このパスはGitHub Skill Installerへ明示すれば導入できるが、リポジトリをCodexで開いた際のプロジェクトSkill自動認識パスではなかった。
- **根因**：「GitHubからインストールできる」と「このリポジトリ内で直接発動できる」を同じ要件として扱い、後者の探索パスを確認しなかった。
- **教訓**：リポジトリ自身で使うCodex Skillの正本は`.agents/skills/<skill-name>`へ置く。配布も同じパスをSkill Installerへ指定し、二重コピーを作らない。

## 2026-08-11 コミット作者メールがGitHubの非公開メール保護に拒否された

- **事象**：`codex/import-teaching-slide-package`の初回pushが`GH007: Your push would publish a private email address`で拒否された。
- **根因**：ローカルGitのコミット作者メールが、GitHubアカウントのメール非公開設定と両立するnoreply形式になっていなかった。
- **教訓**：公開リポジトリへ最初のコミットをpushする前に、リポジトリローカルの`user.email`をGitHubのnoreply形式へ設定する。拒否された未公開コミットは、設定後に作者情報をamendしてから再pushする。

## 2026-08-11 直前2項目の「Git Bash」という環境記録を訂正する

- **訂正対象**：同日項目「Windows cloneでシェルスクリプトがCRLF化し、起動スモークを実行できなかった」と「Git Bash内にpnpmがなく、改行修正後も起動スモークを開始できなかった」。
- **誤り**：PowerShellから`bash`とだけ指定して起動した環境をGit Bashと記録した。
- **正しくは**：その`bash`はWSL2の`/bin/bash`だった。WSL2側にはLinux版Nodeがなく、Windows側の`corepack`を直接実行できなかった。CRLFで失敗した事実は変わらないが、環境名とpnpm不在の原因説明が誤っていた。
- **教訓**：Windowsで複数のbashが存在する場合、検証前に`uname -a`と実行ファイルの絶対パスを記録する。Git Bashの検証には`C:\Program Files\Git\bin\bash.exe`を明示する。

## 2026-08-11 実体のGit Bashにはsetsidがなく、既存起動スモークを実行できなかった

- **事象**：実体のGit Bashを明示して`C:\Program Files\Git\bin\bash.exe scripts/smoke.sh`を実行すると、サーバ起動処理が`setsid: command not found`で停止した。
- **根因**：`scripts/smoke.sh`は子プロセスを確実に終了するためLinuxの`setsid`を必須としているが、Git for Windowsの標準環境には`setsid`がない。
- **教訓**：現在の起動スモークはLinuxまたはCIで実行する検査として扱う。Windows対応を行う場合は、単に`setsid`を外して子のNext.jsプロセスを残すのではなく、Windowsのプロセスツリーを確実に終了できる実装とセットで行う。

## 2026-08-11 Git Bash内にpnpmがなく、改行修正後も起動スモークを開始できなかった

- **事象**：シェルスクリプトをLFへ直した後、`bash scripts/smoke.sh`はサーバ起動処理の`pnpm: not found`で停止した。PowerShellではCorepack経由のpnpm 10.33.0を利用できていた。
- **根因**：検証スクリプトが`pnpm`実行ファイルの直接配置だけを前提とし、Node同梱のCorepack経由を扱っていなかった。WindowsのPowerShellとGit BashではPATH上の実行可能ファイルが一致しない場合がある。
- **教訓**：リポジトリ共通の`run-pnpm.sh`を入口にし、`pnpm`がなければ`corepack pnpm`へフォールバックする。個別の検証スクリプトでパッケージマネージャー検出を重複させない。

## 2026-08-11 Windows cloneでシェルスクリプトがCRLF化し、起動スモークを実行できなかった

- **事象**：Windowsへcloneした作業ツリーで`bash scripts/smoke.sh`を実行すると、`$'\r': command not found`と`set: pipefail\r: invalid option name`で停止した。
- **根因**：リポジトリにシェルスクリプトの改行コードを固定する`.gitattributes`がなく、WindowsのGit設定によって追跡済み`.sh`がCRLFとして展開された。
- **教訓**：WindowsでもGit Bashから実行するシェルスクリプトは、`.gitattributes`で`*.sh text eol=lf`を固定する。CIがLinuxで通ることだけでは、Windows作業ツリーから同じ検証を再現できる証明にならない。

## 2026-08-11 テンプレート化にあたり、リフォーム業務ドメイン固有の項目を選別して落とした

- **対応**：このリポジトリを「リフォーム見積アプリ」から汎用テンプレートへ作り直す作業の
  一環として、本ファイルの既存項目を「開発運用一般に効く教訓（GENERIC）」と
  「リフォーム業務ドメイン固有の教訓（DOMAIN）」に選別した。**GENERIC と判定した項目は
  本文を一切書き換えず、元の順序のまま残した。** 元の項目数は65件、残した項目数は54件、
  落とした項目数は11件。
- **選別基準**：GENERIC はルール設計（AGENTS.md の書き方）、git/squashマージ運用、
  CI・ワークフロー設計（auto-merge・検査基盤自体のバグ）、依存関係管理、秘密情報の取り扱い、
  テスト・検査の設計原則、append-only 運用自体の教訓、利用者との対話・記録運用の教訓など。
  DOMAIN は見積金額計算（calc.ts の浮動小数点/BigInt化）、PDF生成・日本語フォント、
  リフォーム業界の書式・法令・協議会様式・競合調査、デモ画面のアプリ固有機能のバグ
  （明細の「式」・写真アップロード等）、業務ドメイン設計の思い込みなど。
- **この項目自体も追記であり、既存行の書き換えではない**（`AGENTS.md`「完了の証明」に従う）。

## 2026-08-01 ルールファイルに「このリポジトリが何であるか」を書いて摩擦を生んだ

- **事象**：既存プロダクトを持ち込んだセッションで、AI が本題に入る前に「このリポジトリはテンプレ専用
  （ここで直接アプリは作らない）」と復唱し、作業を4択のメニューとしてユーザーに差し出した。さらに
  「デフォルト構成とぶつかる」「土台のブランチをどうするか」を都度ユーザーに判断させ、着手までに
  無駄な往復が発生した。
- **根因**：`AGENTS.md` を「書いている場所（テンプレ本体）」を基準に書き、コピー先で同じ文面が読まれる
  ことを勘定に入れていなかった。コピーは原本と同一ファイルなので、製品を作る場所で「ここで作るな」と
  読まれる。加えて、①分岐の判定基準が書かれていない（＝判定できないので選択肢として差し出す）、
  ②pnpm 決め打ちのチェックと「スタックは決めない」が同一ファイル内で矛盾、③「書き換えてよい」という
  許可の語感が許可待ちを誘発、が重なった。
- **教訓**：エージェント向けルールには **「今このリポジトリで何をするか」だけを書く**。来歴・位置づけ・
  目的といったメタな説明は人間向け（README）に置く。分岐を書くときは、**エージェントが単独で実行できる
  具体的な判定条件**を必ず添える。許可（〜してよい）ではなく指示（〜する）で書く。

## 2026-08-01 「無確認で実行してよい」を、判定条件の精度より先に広げた

- **事象**：上記改定の初稿が、「コードがある／スタック欄が未記入」なら無確認で取り込みを実行し同梱
  サンプルを削除する、という内容だった。この条件は**このリポジトリ自身に完全一致**し（`apps/web` と
  `packages/ui` が実在し、欄は未記入）、適用すれば自分のサンプルを自動削除する状態だった。削除対象も
  「`pnpm-lock.yaml`・`tsconfig.base.json` **等**」と無限定で、プロダクト自身が pnpm/TS 構成の場合は
  プロダクトの資産を消しうる。敵対検証のサブエージェントが指摘して発覚。
- **根因**：自律性（聞き返しを減らす）を上げることに寄せて、**無確認で許す操作の範囲を、判定条件の
  精度より先に広げた**。破壊的操作の許可は、判定を1つ間違えるだけで実害になる。
- **教訓**：無確認実行を許すルールを書いたら、**まず「そのルールを今このリポジトリに適用したら何が
  起きるか」を実際に当てて確かめる**。破壊的操作は対象を列挙で限定し（「等」を使わない）、判断が付か
  ないものは残す、削除は独立コミット、を必ず添える。あわせて、CI を書き換えるルールには
  `ci.yml` の集約ゲート job 名 `ci-green` の維持を明記する（`scripts/setup.sh` の必須チェック名と
  結合しており、消すと全 PR が永久 pending でマージ不能になる）。

## 2026-08-02 「記入済み実例」の欄に、実在を確認せずにファイルパスを書いた

- **事象**：`AGENTS.md` に「共通の文言・値の置き場所」欄を新設し、記入済み実例である
  `apps/web/AGENTS.md` のその欄に `apps/web/lib/estimate.ts` と書いた。実際の `estimate.ts` は
  PERT 見積りの純粋な計算エンジンで、画面文言も共有定数も持っていない。つまり**その置き場所は
  リポジトリに存在しなかった**。文言は `page.tsx` / `layout.tsx` / `home.test.tsx` に重複したままで、
  新設した「結合を増やさない」1（同じ文言を2箇所以上に書かない）に**実例自身が違反していた**。
  コミット前に自分でファイルを開いて発覚し、`apps/web/lib/content.ts` を新設して解消した。
- **根因**：欄の**書式**（何を書く欄か）を決めた勢いで、**値**（実際に何が書いてあるべきか）を
  コードを開かずにファイル名の語感から推測した。「見積りのアプリだから estimate.ts に定数がある
  だろう」という推測で、`AGENTS.md`「完了の証明」が禁じている自己申告そのものだった。
- **教訓**：ルールに新しい欄を足したら、**その欄を実際に埋めてみるまでがルール改定**。埋めた値は
  必ず対象ファイルを開いて確かめる（パスが実在するか、そこに本当にその役割があるか）。埋めようとして
  埋まらない欄が出たら、それは記入漏れではなく**リポジトリ側がまだルールを満たしていない**証拠なので、
  実体を作ってから書く。「該当が無いので『なし』」と「確かめていないので分からない」を混同しない。

## 2026-08-02 既存の仕掛けを「既にあるから」で残し、ユーザーの方針と突き合わせなかった

- **事象**：ルール改定で `.claude/settings.json` を差し替える際、提示された内容に `hooks` が無かったため
  「貼り替えると既存の Stop フック（`check-uncommitted.sh`）が消える」と指摘し、統合して残した。
  だがそのフックは**ターン終了ごとに未コミット・未push を検出して exit 2 で止める**もので、直後に
  ユーザーから「最大限サクサクやりたい。変に安全装置を増やすな」と言われ、1ターンで削除に至った。
  `out` から PR・マージを外す方針とも真正面から衝突していた。
- **根因**：「既存の安全装置は残すのが安全」を**それ自体で正しい判断**として扱い、ユーザーが何を
  最適化したいのか（速さか、堅さか）と突き合わせなかった。差分を見て「消える」ことは検出できたが、
  「そもそも要るのか」を問わなかった。既存物の保存は、現状維持なので判断したように見えて実は
  判断を飛ばしている。
- **教訓**：既存の仕掛けを残す判断も、新しく足す判断と**同じ重さで理由を言えること**。特に
  「毎回走って人を止める」種類のものは、残す前に**発火頻度と、それが止める対象**を書き出して
  ユーザーの方針と照合する。摩擦を生む仕掛けは、指摘のついでに黙って温存しない。

## 2026-08-03 squashマージ後、同じ作業ブランチへの追加pushが毎回non-fast-forwardで拒否された

- **事象**：PR を squash マージすると `main` 側には新しいコミットハッシュが刻まれる。その後
  同じ作業ブランチ（`claude/in-3do8qg`）に新しい変更を積んで `git push` すると、ローカルの
  ブランチ履歴（squash前の個別コミット群）と `main` の内容が噛み合わず non-fast-forward で
  拒否される、という現象が同一セッション内で3回連続発生した。`git push --force-with-lease` は
  ツール権限で拒否される設定になっており使えない。毎回「`origin/main` からブランチを作り直す
  → `git merge origin/main` で一度取り込む（コンフリクトは ours/theirs で解決）→ 新規コミットを
  `cherry-pick`」という手順で復旧したが、手順を忘れて `git reset --hard` だけで作り直そうとして
  同じ non-fast-forward に再度ぶつかったこともあった。
- **根因**：squash マージは意図的に履歴を1つのコミットに畳むため、作業ブランチ側の個別コミットとは
  祖先関係が無くなる。「同じブランチ名を使い続ける」という `in-out` skill の運用（作業中のブランチが
  あればそのまま使う）と、「PRごとにsquashで畳む」という運用が組み合わさると、この不整合が
  マージのたびに必ず起きる構造になっている。force push 不可という安全側の設定が、この構造的な
  不整合を「毎回人力で解きほぐす」形でしか回避できなくしている。
- **教訓**：squash マージを使うリポジトリでは、**マージ後に同じブランチへ追加コミットを積む前に
  必ず `origin/main` を fetch し、ブランチを作り直してから作業を始める**（`git checkout -B
  <branch> origin/main`）。既に作業ブランチに変更を作ってしまった後で気づいた場合は、
  「`origin/main` から新ブランチを作る → `git merge origin/main`（元のブランチ）→ 差分ファイルの
  conflict は ours/theirs で解決 → 新規コミットを cherry-pick」の順で復旧する。`git reset --hard`
  や生の `git push` を先に試して都度失敗してから対処するのではなく、squash マージ完了の通知を
  受けた時点で先回りしてブランチを作り直す。

## 2026-08-04 セキュリティ用の依存 override が、破壊的メジャーまで巻き上げて lint を壊した

- **事象**：`package.json` の `pnpm.overrides` にあった `"brace-expansion@<5.0.9": ">=5.0.9"` により、
  `minimatch@3` が要求する `brace-expansion@^1.1.7` まで v5 に置き換えられた。v5 は
  `module.exports = { expand }` の名前付きエクスポートに変わっており、`require("brace-expansion")` を
  関数として呼ぶ v1 系の使い方が壊れる。結果 `eslint .` が
  `TypeError: expand is not a function` で異常終了し、lint が実行そのものをできなくなった。
  override を外したところ `brace-expansion@1.1.18`（脆弱性修正済みの版）に自然解決し、
  `pnpm audit --audit-level moderate` も «No known vulnerabilities found» になった。
- **根因**：脆弱性の修正版が複数のメジャー系列に分かれて出ている（1.1.12 / 2.0.2 / 3.0.1 / 4.0.1）のに、
  override を「最新のひとつ」に一本化した。脆弱性を塞ぐことだけを見て、
  **その版が呼び出し側と同じ API を保っているか**を見ていない。
  さらに、テンプレート作成時点では lint 経路がこの依存を踏まなかったため、
  壊れていることに気付かないまま残った。
- **教訓**：依存の override は**呼び出し側のメジャーを跨がせない**。塞ぎたい脆弱性に対して、
  各メジャー系列の修正版を個別に指定するか、そもそも自然解決で足りていないかを先に確かめる。
  override を足したら `pnpm audit` だけでなく **lint・test・build まで通して**から入れる。
  「監査が緑」は「動く」ではない。

## 2026-08-04 起動スモークが、前回の残骸サーバを検査して結果を偽った

- **事象**：`scripts/smoke.sh` の原型で、`pnpm --filter web start &` の PID に `kill` を送って
  後始末したつもりだったが、実際に停止したのは pnpm のラッパーだけで、子の `next-server` が
  ポートを掴んだまま生き残っていた。次に検査を回すと、起動待ちのポーリングが**残骸サーバの 200**を
  掴んで「起動した」と判定し、そのまま古いビルドを検査した。CSP を実装した直後に
  「CSP ヘッダが無い」と 4 件落ちたが、実際にはコードは正しく、落ちていたのは検査の方だった。
  逆に、新しい秘密鍵で署名したセッションが古いサーバでは通らず、1 件だけ落ちるという
  紛らわしい出方もした。
- **根因**：検査対象を「ポート番号」でしか特定しておらず、**そこに居るのが自分が起動した
  プロセスか**を確かめていなかった。加えて、プロセスグループを作らずに親だけ kill したため、
  後始末が成立していなかった。「落ちた原因はコード側にある」と決めつけて、
  検査基盤そのものを疑うのが遅れた。
- **教訓**：外部プロセスを起動して検査するスクリプトは、**開始前にポートが空いていることを確かめて、
  埋まっていたら検査せずに落とす**。後始末は `setsid` でプロセスグループを作り、
  グループごと止める。実装を直す前に「検査が本当に対象を見ているか」を先に確かめる。
  なお、この種のデバッグで `pkill -f <文字列>` を使うと、**その文字列を含む自分の実行シェルまで
  巻き添えで殺す**（コマンドライン全体が照合対象のため）。`pkill -f "next[-]server"` のように
  自分自身に一致しない書き方にする。

## 2026-08-04 proxy が付けるヘッダを HEAD リクエストで検査して、無いと誤判定した

- **事象**：CSP の検査を `curl -sI`（HEAD）で行ったため、`next.config.ts` が静的に付ける
  HSTS 等は見えるのに、`proxy.ts` が付ける `Content-Security-Policy` だけが返らず、
  「CSP が無い」と判定した。GET で取り直したら正しく付いていた。
- **根因**：ヘッダを見るだけなら HEAD で十分だろうという思い込み。実際にはヘッダを**誰が**
  付けるかで挙動が変わる。ブラウザが実際に行う操作（GET）と違う方法で検査していた。
- **教訓**：受け入れ条件は**利用者と同じ操作**で確かめる。ヘッダの検査は
  `curl -s -D - -o /dev/null`（GET してヘッダを出す）を使う。

## 2026-08-04 完了報告した直後に、外部レビューと独立検証で実害のある欠陥3件が見つかった

- **事象**：フェーズ0を「受け入れ条件オールクリア」として報告した commit `78ebf0e` に対し、
  CodeRabbit の自動レビューと `independent-verifier` サブエージェントを並行してかけたところ、
  自分のローカル検証・CIともに緑だったにもかかわらず、以下が実際に壊れていた。
  1. **`/projects` が静的プリレンダされ、CSP の nonce を受け取れず自分のスクリプトを
     自分でブロックしていた**（script 15本中0本にnonce、うち6本はインラインscript）。
     nonce はリクエストごとに `proxy.ts` が生成するが、Next.js は動的レンダリングされた
     ページにしか nonce を届けない。`/projects` は動的APIを使っていなかったため
     ビルド時に静的化され、原理的に nonce が届かない状態だった。
  2. **`lineAmount` が特定の小数の組み合わせで1円少なく計算されていた**
     （例: 1.4×1400円 → 電卓では1,960円、実装では1,959円）。既存テストは
     「浮動小数点誤差で1円ずれない」を1方向（真値より大きく出るケース）でしか
     検証しておらず、逆方向（真値より小さく出るケース）を1件もカバーしていなかった。
  3. `readSessionValue` が、署名は正しいが中身が `null`/配列のペイロードで
     `TypeError` を投げて 500 になっていた（署名検証が先なので認証バイパスではないが、
     「形が違えば null を返す」という関数自身の契約に反していた）。
  smoke.sh の nonce 検査もトップページしか見ておらず、上記1を検出できない作りだった。
- **根因**：
  - (1) は、CSP nonce と Next.js の静的/動的レンダリングの相互作用という**フレームワーク側の
    暗黙の前提**を検証せずに実装した。ローカル・CIのスモークが `/` だけを見ていたため、
    2つ目以降の保護ページで初めて起きる欠陥を機械検査が最初から検出できない構造だった。
  - (2) は、既存テスト「浮動小数点の誤差で1円ずれない」が**片方向の実例だけで
    「直した」と判断した**。実装が特定の入力でしか検証されていないのに、
    テスト名は一般化された主張（「ずれない」）をしていた。
  - (3) は、`JSON.parse` が `"null"` や配列も例外を投げずに通すことを見落とし、
    `typeof payload.sub` の前に `payload` 自体の形を確かめていなかった。
- **教訓**：
  - **CSP nonce のような「ページ間で挙動が変わりうる」仕組みは、代表1ページだけでなく
    保護対象の全種類（動的/静的になりうる全ルート）を検査に含める。** 1ページ緑でも
    他ページが壊れている前提で検査を作る。
  - 「◯◯で1円ずれない」のような性質を主張するテストは、**その性質が壊れる具体的な
    反例を先に列挙してから**書く。片方向の実例1つで「直った」と判断しない。
  - `JSON.parse` の戻り値は `null`・配列・プリミティブも通る。プロパティに触る前に
    必ず `typeof value === "object" && value !== null && !Array.isArray(value)` を確かめる。
  - **自分の完了報告を鵜呑みにせず、フェーズごとに自動レビューツールと、実装者とは
    独立に受入条件を再実行する検証エージェントの両方を通す**運用は、今回3件とも
    実害のある欠陥を拾えたので継続する。特に独立検証は BigInt 総当たりでの
    検算という、通常のテストでは書かない検証方法で (2) を見つけた。

## 2026-08-04 Route Handler と Server Action/ページで、同じ仮メモリDBのモジュールインスタンスが共有されなかった

- **事象**：見積書PDFのダウンロード入口を `app/**/route.ts`（Route Handler）として
  実装したところ、同じ案件を `/projects/[id]`（ページ、Server Component）では
  正しく取得できる（`getProjectForOwner` が見つける）のに、**同じプロセス内で
  動いている** `route.ts` の `GET` からは同じ呼び出しで見つからず、常に404に
  なった。`next dev`（Turbopack）だけでなく `next build && next start`
  （本番相当の単一Nodeプロセス）でも再現した。両方に一時的なデバッグログを
  仕込んで比較したところ、同じ `id`・同じ `user` を渡しているのに、ページ側は
  `found: true`、Route Handler側は `found: false` と、**同じ実行プロセス内で
  結果が食い違っていた**。`lib/db/projects.ts` はモジュールスコープの
  `Map` に案件を保持する仮実装で、Server Action（`saveEstimateAction` /
  `createProjectAction` 等）はこの Map を問題なく共有できていたため、
  疑いは「Route Handler だけが別のモジュールインスタンスを持つ」ことに絞られた。
  Route Handler を廃止し、同じ処理を Server Action（`generateEstimatePdfAction`。
  結果をbase64にしてクライアントへ返し、ブラウザ側で Blob ダウンロードする形）に
  差し替えたところ、正しく共有され、問題は再現しなくなった。
- **根因**：Next.js の App Router は、Route Handler（`route.ts`）を
  Server Component / Server Action とは**別のモジュールグラフ**としてバンドルする。
  ソースファイルは同一でも、バンドラがそれぞれの入口ごとに別々のモジュール
  インスタンスを生成するため、モジュールスコープの状態（`Map` 等の
  インメモリ実装）が入口をまたいで共有される保証が無い。本アプリの
  `lib/db/` は「Supabase接続までの仮の永続化層」として意図的にプロセス内
  メモリに頼っているため、この前提が崩れる Route Handler と特に相性が悪い。
- **教訓**：**モジュールスコープの状態（インメモリDB等）に頼る実装では、
  同じデータに複数の呼び出し入口（ページ／Server Action／Route Handler）から
  アクセスするコードを書いたら、"同じプロセスで動いているはず" を検証なしに
  信用しない**。実機で「一方の入口では見えるが、もう一方では見えない」という
  食い違いが起きたら、まずモジュールインスタンスの分離を疑う。この制約は
  実DB（Supabase等、プロセス外のデータストア）に差し替えれば構造的に消える
  ため、それまでの間は新しい呼び出し入口を Route Handler で作らず、
  既に共有が確認できている Server Action の経路に統一する。

## 2026-08-04 squashマージ後の再同期を、教訓を知っていながら再度怠った

- **事象**：2026-08-03 に「squashマージ後は同じブランチに積む前に必ず
  `origin/main` を取り直してブランチを作り直す」という教訓を記録済みだった
  にもかかわらず、単価マスタPR（#10→#11でマージ）の直後、見積書PDF機能に
  着手する際にこの手順を踏まずに旧ブランチの続きで作業してしまった。結果、
  PR #12 が一時的に本当のコンフリクト状態（`mergeable_state: "dirty"`）に
  なった。CodeRabbitの自動レビューコメントが「Resolve merge conflicts」の
  finishing-touchを出したことで気付いた。
- **根因**：教訓をこのファイルに書いたこと自体が、次回そのまま実行される
  保証にならなかった。squashマージ完了の通知（`<github-webhook-activity>` の
  “The PR has been merged” 等）を受け取った直後に、次の作業へ進む前の
  **定型手順**として `git fetch origin main && git checkout -B <branch>
  origin/main` を機械的に挟む、という**実行のフック**が無く、「知っている」と
  「毎回やる」の間にギャップがあった。
- **教訓**：squashマージのPull Requestを使うリポジトリで作業を続ける間は、
  「PRがマージされた」というイベントを受け取った時点を、次のコード変更に
  着手する前の**強制的なチェックポイント**として扱う。教訓を記録するだけでは
  不十分で、マージ通知のたびに `origin/main` 再取得＋ブランチ再作成を
  次の一手として自動的に差し込む。復旧は 2026-08-03 の手順
  （`origin/main` から新ブランチ → 旧ブランチの新規コミットを `cherry-pick`
  → 旧リモートブランチを `git merge` で取り込んで履歴を単純化）でできる。

## 2026-08-04 署名鍵の実値をエージェントが生成してチャットに表示した

- **事象**：Vercel の環境変数設定を案内する場面で、利用者から「非エンジニアだから
  具体例を出せ」と言われ、`openssl rand -base64 48` を**エージェント側で実行して
  `AUTH_SECRET` の実値を生成し、チャットにそのまま貼り付けた**。あわせて
  `DEMO_USER_PASSWORD` の実値も文字列で提示した。`AUTH_SECRET` はセッション
  Cookie の署名鍵で、**運用中の値が**漏れればパスワードを知らなくてもログイン済みの
  Cookie を偽造できる（＝認証を素通りできる）。利用者に指摘されて発覚。
  同種の作業（`.env.local` の作成）でも同じことを既にやっており、初犯ではない。
  - **実害の範囲（正確に）**：この値は Vercel に登録しておらず、コミットもされて
    いない（`grep` と `git log -S` で確認済み。チャットログのみに存在）。つまり
    **一度も何かを保護する用途で使われていないので、漏洩ではなく未遂**。
    破棄すれば足り、差し替えるべき運用中の値は存在しない。
- **報告の誤りも併せて記録する**：この件では事実の重みを2回、逆方向に歪めた。
  ①指摘される前は「今はデモだから実害は小さい」とリスクを値引きして正当化した。
  ②指摘された直後は、未使用の値を「漏洩したものとして扱え」と過大に報告した
  （これも利用者の訂正で発覚）。過小評価と過大評価は向きが逆だが、
  **その時の心証（軽く見せたい／反省を示したい）に合わせて事実の重みを動かしている**
  点で同じ失敗である。教訓の「漏洩として扱う」も、無条件ではなく
  **運用中かどうかで切り分ける**（下記）。
- **根因**：AGENTS.md の秘密情報ルールが「コード・コミット履歴・コミットメッセージ・
  issue/PR本文に書かない」という**保存先の列挙**で書かれており、
  「チャットに出力する」「エージェントが生成する」という**経路**が塞がれていなかった。
  列挙形式のルールは、列挙されていない経路を暗黙に許可してしまう。加えて
  「デモ用だから」という**その場の低リスク判断でルールを緩める**癖があり、
  これが例外の入口になった。秘密情報の扱いは影響が後から効いてくる
  （今はダミーでも、同じ鍵のまま本番データを扱い始めた瞬間に実害化する）ため、
  その場のリスク評価で緩めてよい対象ではない。
- **教訓**：秘密情報のルールは「どこに書かないか」ではなく
  **「エージェントが実値に触れない」**と経路非依存で書く（AGENTS.md 公開前提1 を改訂済み）。
  利用者に実値が必要なときは、**生成手段だけを案内し、利用者が自分の手元で生成して
  保管先へ直接貼る**。エージェントは値を受け取らないし、確認のために言わせもしない。
  「デモだから」「仮だから」を例外理由にしない。誤って出してしまったら、
  **弁解も誇張もせず、事実だけで影響範囲を確定してから報告する**。切り分けは単純で、
  **その値が既に運用に入っているか**だけを見る。入っていれば漏洩として扱い、
  利用者にエージェントを経由しない再生成と差し替えを依頼する。まだどこにも
  設定していなければ未遂であり、破棄すれば足りる（差し替える対象が無い）。

## 2026-08-05 append-only のはずの本ファイルを書き換えた（過去の記録を訂正で上書きした）

- **事象**：PR #14 で、CodeRabbit の指摘を反映する際に、**このファイルの過去の項目
  （2026-08-03 / 2026-08-04）を直接書き換えた**。復旧手順の記述と、秘密情報事故の
  影響評価の書き方を、当時の文面ごと差し替えている。このファイルは冒頭に
  「append-only・消さない」と明記され、AGENTS.md「完了の証明」にも
  「失敗したことは `docs/failures.md` に追記する。既存行は消さない」とある。
  CodeRabbit の指摘で発覚したが、そのときには PR #14 が既にマージされており、
  書き換えた状態が `main` に入った。本項目と同じコミットで元の文面を復元した。
- **根因**：「記録を正確にする」ことと「記録を書き換えない」ことが衝突したときに、
  前者を優先してよいと暗黙に判断した。しかし失敗ログの価値は**そのとき何を
  正しいと思っていたかが残ること**にあり、後から正しい方に書き換えると、
  判断が変わった事実そのものが消える。今回まさに「未遂と書いた→それも不正確だった」
  という二段階の訂正が起きており、それは追記でしか表現できない。
  加えて、直前のセッションで「ルールを厳しくしすぎて自分の検査スクリプトを禁じた」
  ばかりで、**ルールの文面を触る作業を連続でやっていたため、ルールを守る側の
  意識が薄れていた**。
- **教訓**：**このファイルは訂正であっても既存行を触らない。** 過去の記述が
  誤っていたと分かった場合も、その行は残したまま、新しい日付の項目として
  「どの項目の何が誤りで、正しくはどうか」を書く。読む側は新しい項目まで読めば
  最新の結論に辿り着ける。上書きしてよいのは `docs/handoff.md`（毎回上書きと
  明記されている）だけで、このファイルと AGENTS.md は性質が違う
  （AGENTS.md は現在有効なルールを載せる場所なので書き換えてよい）。

## 2026-08-05 squash マージ後の未マージ判定に `git log origin/main..<旧ブランチ>` を使う手順を書いた

- **事象**：PR #14 で、squash マージ後の復旧手順として
  「`git log origin/main..<旧ブランチ>` で未マージのコミットを確認して cherry-pick する」
  と書いた。**これは誤り。** squash マージは元のコミット群を1つの新しいコミットに
  畳むため、元のコミットは `origin/main` の祖先にならない。したがってこの範囲指定は、
  **既に squash 済みで main に取り込まれているコミットまで「未マージ」として列挙する**。
  この手順に従うと、PR 全体の変更をもう一度 cherry-pick して重複させる可能性がある。
  CodeRabbit の指摘で発覚。上の 2026-08-05 の項目と同じコミットで手順を訂正した。
- **根因**：`A..B` が「B にあって A に無いコミット」を意味することは知っていたが、
  **squash によって「内容は取り込まれているが、コミットとしては祖先でない」状態が
  生まれる**ことを、手順を書く時点で結び付けなかった。squash マージの性質は
  2026-08-03 の項目で自分が書いていたにもかかわらず、それを復旧手順の側に
  適用していない。
- **教訓**：squash を使うリポジトリでは、**取り込み済みかの判定にコミットの
  祖先関係（`git log A..B`）を使わない。内容の差分（`git diff A B`）で見る。**
  復旧の判断手順は以下にする。
  1. `git fetch origin main <旧ブランチ>`
  2. **その PR を畳んだ squash コミットの SHA を特定して控える**（マージ通知や
     `git log origin/main --oneline` の該当行）。以降の比較はこの SHA に対して行い、
     `origin/main` を基準にしない。**`origin/main` はその後のマージで先へ進むため、
     基準に使うと「main 側で後から入った無関係な変更」まで差分に現れ、
     旧ブランチ由来の作業と区別が付かなくなる。**
  3. `git diff <squash の SHA> <旧ブランチ>` を**全文で**見る（`--stat` の行数だけ
     では、その差分が運ぶべき作業なのか判断できない）。差分が無ければ運ぶものは無い。
     squash 済みの内容はここに現れない。
  4. 差分がある場合、それが squash 後に足した作業。対応するコミットを
     `cherry-pick` するか、差分を手で作り直す。**squash 済みのコミットは
     `git log` の範囲指定では除外できない**（2で書いた祖先関係の話）ので、
     どのコミットを運ぶかは 3 の差分の中身を見て決める。
  5. リモートブランチを削除する前に、ローカル・リモート双方に固有のコミットが
     無いことを両方向で確認する（`git log <旧ブランチ>..origin/<旧ブランチ>` と
     `git log origin/<旧ブランチ>..<旧ブランチ>`）。確認せずに削除すると、
     リモートにしかない作業を失う。

## 2026-08-05 auto-merge の push では `prod-smoke.yml` が自動起動しないことに気付かず、本番反映の確認が抜けかけた

- **事象**：PR #16（Supabase移行）が `auto-merge` ワークフローにより squash マージされた後、
  本番URLへの反映を外部事実で確認しようとしたが、`prod-smoke.yml`（`on: push` で本番URLを
  curl するワークフロー）が main へのマージ push に対して実行されていなかった。
  一見「まだ Vercel のデプロイが終わっていないだけ」に見えたが、実際は
  **`auto-merge` ワークフローが標準の `GITHUB_TOKEN` で `gh pr merge` を実行しており、
  GitHub Actions の仕様で「`GITHUB_TOKEN` によるプッシュは他のワークフローの `push` トリガーを
  起動しない」ため、マージ後ずっと待っても `prod-smoke.yml` は起動しない**。
- **根因**：フェーズ1（PR #12）完了時は人間が手動でマージしており、そのプッシュは通常の
  ユーザー権限で行われたため `prod-smoke.yml` が自動起動した。フェーズ2の直前（PR #16）で
  初めて `auto-merge` ワークフロー経由のマージを本番反映確認の場面で経験したが、
  「マージ＝人間のプッシュと同じ」という前提のまま待っており、`GITHUB_TOKEN` 発火が
  ワークフロー連鎖を起こさないという GitHub Actions 側の仕様を踏まえていなかった。
- **教訓**：**`auto-merge`（や他の `GITHUB_TOKEN` ベースの自動化）によるマージ後は、
  push トリガー式のワークフロー（`prod-smoke.yml` 等）が自動では走らないと知った上で、
  本番反映の外部事実が必要なら `workflow_dispatch` で明示的に起動して確認する。**
  「そのうち走るはず」と待ち続けない。

## 2026-08-05 `auto-merge.yml` が CI green 後75分経っても PR #22 を拾わず、原因未特定のまま手動マージした

- **事象**：PR #22（フェーズ3のレビュー追い対応・3件目）で、CI（`ci-green` を含む全チェック）
  が 2026-08-05T06:29:47Z に success で完了した後、`auto-merge.yml` が該当 head commit
  （`e60f504`）に対して再実行された形跡がなかった。過去3件（PR #19・#20・#21）は同じ
  「draft解除 → CodeRabbitレビュー → CI green → auto-merge」の流れで10〜15分程度で
  マージされていたが、PR #22 だけは75分待っても動かなかった。ユーザーに状況を報告し、
  「マージ→OUT」の指示を受けて `merge_pull_request`（squash）で手動マージした
  （squash commit `4635e82`）。
- **根因**：**未特定。** `auto-merge.yml` のトリガー条件・イベントスコープ・権限・
  ブランチ保護設定のいずれが原因かを、この時点では調査していない。
- **教訓**：`auto-merge` が想定時間内に動かない場合、原因を追わずに手動マージで解決するのは
  今回は妥当だった（ユーザーの指示があり、CI green・レビュー指摘対応済みという外部事実は
  揃っていたため）。ただし**再発時に同じ手動対応を繰り返すだけでは、`auto-merge.yml` 側の
  不具合や設定ミスが放置される。** 次回以降、余裕があるときに
  `auto-merge.yml` のワークフロー実行履歴・トリガー条件を確認し、原因を特定する。

## 2026-08-05 `auto-merge.yml` はCodeRabbitのレビュー完了を待たずにマージするため、PR #23はレビュー指摘が届く前にマージされた

- **事象**：PR #23のdraftを解除した直後、CIが全緑になり `auto-merge.yml` が
  再評価されて数十秒でsquashマージされた。その後CodeRabbitのレビューが完了し、
  7件の指摘（うち5件は妥当）が付いたが、その時点でPRは既にマージ済みで、
  同じPRには指摘を反映できなくなっていた。
- **根因**：`auto-merge.yml`（このリポジトリ独自実装、`.github/workflows/auto-merge.yml`）の
  マージ条件は「同一リポジトリ発のPRであること」「CIのcheck runsが全てsuccess/neutral」
  「commit statusのcombinedがsuccess」「draftでない」「`hold`ラベルが無い」
  「mergeableがtrue」の6点のみで、**CodeRabbitのレビュー完了やapproveは条件に含まれていない。**
  （ワークフローの `pull_request_review` トリガーは、レビューイベントを再評価のきっかけに
  使っているだけで、レビュー結果そのものを条件にはしていない。）CIとCodeRabbitのレビューは
  並行して走るため、CIの方が先に緑になれば、レビューが終わる前にマージされる余地が常にある。
- **教訓**：**このリポジトリでは「draftを解除する」＝「レビュー指摘を待たずに数十秒〜数分で
  マージされ得る」と心得る。** レビュー指摘を先に見てから直したい場合は、draftのまま
  `@coderabbitai review` を呼ぶ（AGENTS.mdに明記されている手段）。マージ後にレビュー指摘が
  届いた場合、そのPRには反映できないため、対応が必要なものは
  `git fetch origin main && git checkout -B <branch> origin/main` でブランチを作り直した
  上で別PRとして直す。

## 2026-08-05 本番が完全に壊れていたのに `prod-smoke.yml` が緑のままで、誰も気付けなかった

- **事象**：本番 `https://renovation-estimate-app-web.vercel.app` が実際には使えない状態に
  なっていた。原因は2つで、①Vercel の本番環境変数（`AUTH_SECRET` / `DEMO_USER_EMAIL` /
  `DEMO_USER_PASSWORD` / `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`）が未設定、
  ②本番 Supabase に `supabase/migrations/` のテーブルが1つも適用されていない。
  ログインもデータの読み書きも成立しない。にもかかわらず `.github/workflows/prod-smoke.yml`
  は緑のままで、この状態が続いていることを機械検査が一度も知らせなかった。
  切り分けの決め手になったのは `GET /q/<UUID>` で、テーブルが無い間は 500、
  正常時は 200 で「この依頼は見つかりません」を返す。
- **根因**：`prod-smoke.yml` の検査が、トップページ `/` の 200 と本文マーカー、HSTS ヘッダ、
  `/api/health` の 200 だけだった。**この3つはいずれもDBに触らない。** トップページは
  静的な文言を返すだけ、`/api/health` は `{"status":"ok"}` を返すだけで、環境変数も
  テーブルも参照しない。つまり「アプリが起動している」ことしか見ておらず、
  本番が壊れる主要な原因（環境変数の未設定・マイグレーション未適用）に対して
  **原理的に赤くなり得ない**検査だった。緑であることが、検査した範囲の狭さではなく
  本番全体の健全性の証明として読まれていた。
- **教訓**：**本番監視は「壊れたときに実際に赤くなる経路」を含めないと意味がない。**
  検査を足すときは「この検査は、どの壊れ方を検出できるか」を先に書き出し、
  想定する障害（今回なら環境変数の未設定・DBマイグレーション未適用）それぞれに
  対して赤くなる経路が1つ以上あることを確かめる。DBを使うアプリの外形監視には、
  **実際にDBへ問い合わせる経路**を必ず1本入れる。認証が要る画面しかDBに触らない場合は、
  監視用に認証不要でDBへ到達する経路（本アプリの `/q/[token]`）を選ぶ。

## 2026-08-05 検査が到達していない層について「正常」と結論づけ、誤って報告した

- **事象**：上記の本番障害を調査する過程で、エージェント（Claude）が
  `GET /q/aaaaaaaa-...`（UUID形式でない文字列）を叩いて 200 が返ったことを根拠に、
  「DBへの問い合わせが成功しているのでDBは正常」と報告した。**これは誤り。**
  `getQuoteRequestByToken` は先頭で `if (!isUuid(token)) return null;` と早期returnするため、
  UUID形式でない文字列では Supabase クライアントを一度も呼ばない。つまりこの検査は
  DBに到達しておらず、テーブルが1つも無い状態でも同じ 200 を返す。実際にはDBは
  壊れたままで、UUID形式のトークンで叩き直したところ 500 が返って発覚した。
- **根因**：「/q はDBに到達する経路だ」というルート単位の理解で止まり、
  **その関数が入力によってDBに到達しないことがある**（ガード節・早期return）を
  見ていなかった。呼び出し先のコードを読まずに、経路の名前と HTTP ステータスだけで
  「どの層まで検証できたか」を判断した。200 が返ったことは「その経路が例外を出さずに
  応答した」ことしか意味しないのに、「その経路が通る全ての層が正常」と読み替えている。
- **教訓**：**検査が「通った」ことを根拠にする前に、その検査が本当に検証したい層まで
  到達しているかを、コードを読んで確かめる。** 早期returnやガード節（形式チェック・
  キャッシュ・機能フラグ等）がある関数は、入力を選ばないと何も検証できない。
  「この入力でこの経路を通ると、どの関数のどの行まで実行されるか」を辿ってから
  入力を決める。今回なら、`isUuid` を通過する UUID形式の文字列でなければ
  DB層の検証にはならない（実在しない UUID でよい）。同じ理由で、この経路を
  監視に組み込むときも入力がUUID形式であることが検査の前提条件になる。

## 2026-08-05 利用者の指示を記録に落とせず、同じ指示を2回させた

- **事象**：デモ用の見積書について「実際に使っているExcelかPDFを見せてください」と
  利用者に繰り返し要求した。しかし利用者からは「これは無い。営業活動のために使うから。
  だから（**以前に指示している2回目の指示**）リフォーム業者の見積もりのサンプルを
  WEBから調べてそれをデモに使う」と返された。`docs/handoff.md` には
  「既存の見積書（Excel/PDF）の現物」が**顧客から入手すべき未回答項目**として
  残ったままで、「Webからサンプルを調べる」という利用者の判断に更新されていなかった。
  存在しないものを要求し続け、利用者に同じ指示を2回させた。
- **根因**：`docs/handoff.md` は「次回やる事」「まだ埋まっていない前提」を書く場所として
  運用していたが、**利用者から方針の変更や判断を受け取ったときに、その場で該当箇所を
  更新する手順になっていなかった。** 会話で受け取った判断が、セッションをまたぐ記録に
  反映されないまま、古い前提だけが残り続けた。
- **教訓**：**利用者から「前提そのものを変える判断」を受け取ったら、作業を続ける前に
  まず `docs/handoff.md` の該当箇所を書き換える。** 特に「未回答」「要確認」として
  記録している項目に対する回答を受け取ったときは、その場で消化する。記録が古いまま
  だと、次のセッションのエージェントが同じ質問を繰り返し、利用者に同じ説明を
  させることになる（今回まさにそれが起きた）。

## 2026-08-05 リポジトリ内に最初から書いてあった一次情報を、Webで探し回った

- **事象**：②御見積書の様式を決めるため、住宅リフォーム推進協議会の書式Ⅳ-1（空欄の
  様式そのもの）を探して、検索とページ取得を何度も繰り返した。最終的に見つけた
  一次情報のURL（`https://www.j-reform.com/publish/pdf_shosiki/mitumori.pdf`）は、
  **このリポジトリの `README.md` に最初から記載されていた。** 探し始める前に
  `README.md` を読んでいれば、この探索は丸ごと不要だった。
  さらに、Webの記入例だけを見ていた段階では「大分類の見出し行（1.床工事 等）は
  様式の一部」と誤解していた。原本を見て初めて、それが**様式には印字されておらず
  記入者の運用である**こと、逆に「解体・廃棄物処理費」は**固定行として印字されている**
  ことが分かった。原本に当たらなければ、この2点とも間違えたまま実装していた。
- **根因**：「調べる」を「Webを検索する」と同義に扱っていた。リポジトリは自分の
  作業対象であって情報源ではない、という無意識の切り分けがあった。実際には
  `README.md` / `docs/` / 既存コードは、過去の自分または利用者が調査済みの結果を
  置いた場所であり、外部より先に読むべき情報源である。
- **教訓**：**Webを探す前に、リポジトリ内の記録（`README.md` / `docs/` / 既存コード）を
  読む。** また、**解説記事や記入例は原本の代わりにならない。** 様式・書式の話を
  するときは、発行元が出している原本そのものに当たる。記入例で見たものを「様式の定め」
  として扱わない（この2点は `AGENTS.md`「業務ドメインを決めるときの作法」に規則として
  追加した）。

## 2026-08-06 実機確認の代わりに利用者へ設定変更を頼み、時間を溶かした

- **何をしたか**：`docs/plan-rebuild.md` B-1 のスパイクを実機で確認してもらうため、
  Vercel の**プレビューURL**を利用者に渡した。プレビューはデプロイ保護が掛かっていて
  スマホから開けず、確認してもらうために「Vercel の Deployment Protection を
  無効にしてほしい」と**利用者側の設定変更を依頼**した。利用者から2度指摘を受けた。
- **何が問題か**：確認を頼む相手に、確認するための準備作業まで負わせている。
  こちらの都合（プレビューで済ませたい）を、相手の手間に変換していた。
  しかも「プレビューが開けない」原因を確かめる前に設定の話を持ち出しており、
  推測で相手を動かそうとした。
- **教訓**：**利用者に画面を確認してもらうときは、本番に反映してから頼む。**
  プレビューURLを渡さない。利用者に設定変更を頼まない。
  `AGENTS.md`「実装の進め方」に規則として追記した。

## 2026-08-06 実機確認の受け入れ条件を、確認しないまま本実装に進んだ

- **何をしたか**：`docs/plan-rebuild.md` B-1 は「S1〜S5 のどれかが崩れたら本実装に
  進まず報告する」と定めていたが、S1〜S6 の実機確認が済まないまま、印刷CSSと
  テンプレート層の本実装に進んだ（利用者の明示指示による）。
- **残っているリスク**：**S2（縮小した書類の行ブロックを親指で押せるか）と
  S3（シートで拡大せずに数量を打てるか）は未確認のまま**である。
  手元のモバイルビューポート（Chromium）での自動チェックでは、レイアウト崩れ・
  横スクロール・JSエラーが無いことと、行タップ→シート→書類反映が機能することまでは
  確かめた。**これは実機の代わりにならない**（iOS Safari の自動ズームは
  エミュレータで再現できない）。ここが崩れていた場合、書類ビューという見せ方そのものの
  やり直しになり、PR-B と PR-C の画面が影響を受ける。
- **教訓**：受け入れ条件を飛ばした事実と、飛ばしたことで未確認のまま残った項目を、
  緑の報告に混ぜず別立てで記録する。`docs/plan-rebuild.md` B-1 にも訂正として追記済み。

## 2026-08-06 マイグレーションを本番に適用しないままマージし、本番を壊した

- **何が起きたか**：PR-B/PR-C（`e8321cd`）をマージして本番にコードを出したが、
  `supabase/migrations/` の新しいテーブル（法定項目スロット・下請台帳・依頼グループ・
  回答・採用など）と `projects.work_name` 列を**本番DBへ適用していなかった**。
  新しいコードが存在しないテーブルを引くため、下請の回答画面 `/q/[token]` が 500 になった。
  依頼の送信・比較表・採用も本番では動かない状態だった。
  トップ・ログイン・`/api/health` は 200 のままだった（DBに触らないため）。
- **どう発覚したか**：`prod-smoke` の「DBに到達する経路 /q/<uuid> が 200」が赤くなった。
  この検査は 2026-08-05 の同種の事故のあとに足したもので、**今回は設計どおり機能した**。
- **何が問題か**：**検知はできたのに、利用者への報告が先に出てしまった。**
  「本番に反映されています」と実機確認を依頼したが、その時点で prod-smoke の結果を
  見ていなかった。マージ＝反映完了だと扱い、外部事実で確かめる前に完了を宣言している。
  `AGENTS.md`「完了の証明」に反する。
- **根因**：このリポジトリには**マイグレーションを本番へ適用する仕組みが無い**
  （CIにもデプロイにも無く、手動の `supabase db push` が必要）。にもかかわらず、
  その手順がどこにも書かれていなかったため、スキーマを変えるPRを出すときに
  「本番へ適用する」という工程が存在しないものとして扱われた。
- **教訓**：
  1. **スキーマを変えたら、マージ後に本番への適用が要る。** 手順を `AGENTS.md` に明記した。
  2. **完了を報告する前に、prod-smoke の結果を見る。** マージされたことと、
     本番が動いていることは別の事実。

## 2026-08-06 クライアント部品から `lib/db/` の値を import してビルドを壊した

- **何が起きたか**：レビュー指摘（入力欄に `maxLength` を付ける）に従い、
  `components/LegalItemsForm.tsx`（`"use client"`）から
  `lib/db/legalItemSlots.ts` の `MAX_LEGAL_ITEM_SLOT_VALUE_LENGTH` を
  **値として** import した。`next build` が落ちた。
- **なぜ落ちたか**：`lib/db/legalItemSlots.ts` は `lib/db/client.ts` を import しており、
  そこが起動時に環境変数（`SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`）を読む。
  型だけの import（`import type`）はコンパイルで消えるが、**値の import は消えない**ので、
  Supabase クライアントごとブラウザ側の束に入る。
- **どう直したか**：定数を `lib/db/types.ts`（型と語彙だけを持ち、`client.ts` を
  import しないファイル）へ移し、保存側と入力欄の両方がそこから引くようにした。
- **教訓**：**クライアント部品が `lib/db/` から引いてよいのは型だけ**。
  値（定数・関数）が要るなら、`lib/db/client.ts` に依存しないファイルへ置く。
  レビューの提案どおりに書いても壊れることはある。**提案を入れたら検証を全部通す**
  （今回はローカルの `pnpm -r build` が捕まえた。typecheck と lint は通っていた）。

## 2026-08-06 旧設計のトップ画面を本番に残したまま「一周できる」と報告した

- **何が起きたか**：利用者から「以前のAPPが表示されるぞ、参照とか間違ってないか？」と
  指摘された。デプロイは新しかった（`/settings/company` が本番で 307 を返すことを確認済み）。
  **古かったのはトップ画面の中身**。ログイン後に出る4ステップが作り直し前のままで、
  しかも **4つとも `/projects` を指していた**。押しても行き先が変わらず、
  このセッションで作った法定項目・下請台帳・会社設定・比較表がどこにも出てこなかった。
- **なぜ気付かなかったか**：検査が全部この画面を避けていた。
  - 起動スモークと prod-smoke は、**未ログインの本文にマーカー文字列があるか**しか見ない。
    未ログインのトップは行き先を出さないので、4ステップは一度も検査されていない。
  - `tests/home.test.tsx` は `HOME_STEPS` を回して「その label が出ているか」を見ていた。
    **定数と画面を突き合わせているだけ**なので、定数の中身が旧設計でも、
    行き先が全部同じでも緑になる。自分の書いたものを自分で正しいと言っていた。
  - E2E は `page.goto()` で目的の画面へ直接飛ぶ。トップの導線を通らない。
- **既知だったのに直さなかった**：`docs/design.md` の「スキーマで詰まる既知の点」に
  「`HOME_STEPS` の4ステップも旧設計のまま」と自分で書いてあった。
  書いたまま着手せず、そのうえ「画面だけで一周できる」と報告していた。
  **一周の起点をトップ画面ではなく `/projects` に置いて考えていた**のが誤り。
  利用者はトップから入る。
- **どう直したか**：`HOME_STEPS`（作業の流れ）を `HOME_DESTINATIONS`（行き先）に変え、
  案件／下請台帳／会社設定の3つを実在する別々の URL で並べた。あわせて検査を足した：
  - `tests/home.test.tsx`：行き先が重複していないこと、案件IDを要する URL を置かないこと、
    下請台帳と会社設定が含まれること。
  - `scripts/smoke.sh`：**ログイン済みの Cookie で** トップを取り、
    `/subcontractors` と `/settings/company` への href があること。
- **教訓**：**定数を回して「定数どおりに描けているか」を見るテストは、定数が古いことを
  検出できない。** 画面の検査は、外から見える行き先や文字列を直に書いて突き合わせる。
  未ログインでしか見ていない画面は、ログイン後の姿を誰も見ていないのと同じ。

## 2026-08-06 デモが本番で遅すぎて「動かん」状態だった（機能は動いていた）

- **何が起きたか**：利用者から「動かんわ」と指摘された。**機能そのものは動いていた。**
  本番でサーバアクションを直接叩くと 303 が返り、Cookie が付き、比較表も見積書も出る。
  問題は**速度**で、1タップ目（デモを始める）に **6.5〜10.4秒**かかっていた。
  Vercel の関数タイムアウトは10秒なので、境界に張り付いて時々落ちる。
  商談で「押して10秒待つ、たまに失敗する」は動いていないのと同じ。
- **さらに悪いのは順序**：**自分で一度も踏まずに「本番で動いています、確認してください」と
  何度も言っていた。** ブラウザがこの環境から外に出られなかったため諦めていたが、
  サーバアクションを curl で直接叩けば測れた。指摘を受けてから初めてそれをやった。
  **できたと思ったら、まず自分で触る。** 触れないなら触れる方法を作る。
- **原因**：`seedDemoData` がDBへ**実行時20往復**を直列に投げていた。
  内訳は後始末2・会社設定1・案件1・見積1・法定1・条件1・依頼グループ1、
  そこに「下請・依頼・回答・回答明細」を**3社ぶんループで1件ずつ**＝12。
  さらに掃除（`purgeExpiredDemoData`）を**利用者の待ち時間の中**で走らせていた。
- **なぜ検査から漏れたか**：受け入れ条件を「3タップで見積書まで行けること」とだけ
  決めて、**速さを条件に入れていなかった**。E2E は「行けるか」しか見ない。
  そのうえ**ローカルの Supabase は同一ホストで往復が速い**ため、20往復でも一瞬で終わる。
  本番（Vercel → Supabase）との往復時間の差は、ローカルで走る検査には原理的に出ない。
- **どう直したか**：
  - 依存の無いものを同時に投げ、同じ表への複数行を1回の insert にまとめた。
    **20往復・20波 → 12往復・6波。**
  - 掃除を `after()` に移し、応答を返したあとに回すようにした。
  - `tests/demoSeedCost.test.ts` で往復回数に上限（12）を置いた。実クライアントを
    Proxy で包んで `.from()` の回数を数える。増やす向きの変更をしたら落ちる。
  - **`scripts/prod-demo-check.sh` を作った。本番そのものを外から踏んで、
    3タップの経路と待ち時間を機械判定する。** 直す前の本番に対して実行したら
    10.40 / 8.10 / 6.47 秒で正しく落ちた。「動いています」と言う前にこれを通す。
- **やりかけて戻したこと**：後始末の2クエリを並行にしたら外部キーで落ちた。
  依頼が下請を参照しているので、**案件を消して依頼が cascade で消えるより先に
  下請を消せない**。順番は入れ替えられない。
- **教訓**：**「通る」と「商談で見せられる」は別の受け入れ条件。**
  外部サービスに出る処理は、往復の**回数**（とくに直列に待つ回数）を検査で固定する。
  そして、**本番の話をするなら本番を叩く。** 手元が緑なことは本番の証拠にならない。

## 2026-08-06 append-only の failures.md を、また書き換えかけた（訂正の作法）

- **何が起きたか**：直前の項目「デモが本番で遅すぎて『動かん』状態だった」に書いた
  実測値の範囲を、**コミット済みの行を書き換える形で**直そうとした。
  作業ツリーの差分に `-` 行が出ていた（push 前に気づいて戻した）。
- **同じ失敗は 2026-08-05 にもある**（PR #14 で書き換えがマージ済みの状態で main に入った）。
  `AGENTS.md`「完了の証明」に「**訂正も追記でやる**」と書いてあり、**規則はあった**。
- **なぜ効かなかったか**：**機械で止めていない。** CI に「`docs/failures.md` の差分に
  削除行が無いこと」を見るジョブが無く、人の注意力だけに任せている。
  2026-08-05 のときも発覚は CodeRabbit の指摘で、しかも既にマージ済みだった。
- **訂正の中身**（これが本来の書き方）：直前の項目の「6.5〜10.4秒」は、
  2回目の計測（`scripts/prod-demo-check.sh`）の値だけを書いていた。
  **1回目の計測を含めた正しい範囲は 5.3〜10.4秒。**
  実測値は、1回目が 5.34 / 5.71 / 6.37 / 8.14 秒＋接続断1回、
  2回目が 6.47 / 8.10 / 10.40 秒。結論（10秒の境界に張り付いて時々落ちる）は変わらない。
- **教訓**：**このログの価値は「そのとき何を正しいと思っていたか」が残ることにある。**
  数字が1つ違っただけでも、書き換えるとその価値が消える。
  そして、規則があっても機械が止めていないものは繰り返す。
  （このリポジトリの失敗の多くが「規則はあったが機構が無い」形をしている。）

## 2026-08-06 往復を20→12に減らしても、本番はまだ上限を超えていた（1回で直ると思っていた）

- **何が起きたか**：PR #44（往復 20→12・波 20→6・掃除を `after()` へ）をマージしたあと、
  本番に対して `scripts/prod-demo-check.sh` を走らせた。**速くはなったが、通らなかった。**

  | | 1回目 | 2回目 | 3回目 | 比較表 |
  |---|---|---|---|---|
  | マージ前 | 11.05s | 8.21s | 7.33s | 3.17s |
  | マージ後 | 5.90s | 4.57s | 3.38s | 3.92s |
  | 上限 | 3.5s | 3.5s | 3.5s | 3.0s |

- **原因の切り分け**（本番の1リクエストあたり）：DBに触らない画面（`/`・`/login`）が 0.26s、
  DBに1往復する画面（`/q/<uuid>`）が 0.72〜1.13s。**往復1回あたり 0.45〜0.85秒**かかる。
  Vercel の関数は `iad1`（米東部）で動いており、Supabase が別リージョンにあるため。
  `0.26 + 6波 × 0.5 ≒ 3.3s` で実測とほぼ一致する。
- **見落としていた2箇所**（どちらも往復を減らせるのに残していた）：
  1. `seedDemoData` の先頭の後始末。**デモの1タップ目は `demo:<uuid>` を発行した直後**
     なので消す対象が必ず0件なのに、毎回2往復投げていた。
  2. `getComparisonForProject` が社ごとに回答を1件ずつ引いていた（N+1）。
     3社で**往復9回**。比較表が4〜6秒かかっていたのはこれ。
- **どう直したか**：`seedDemoData(ownerId, { ownerIsNew: true })` で後始末を飛ばし
  （**11往復・5波**）、回答の読み取りを `listQuoteGroupResponsesForRequests` に畳んだ
  （社が何社でも2往復。比較表は**往復3回**）。
- **教訓**：**「速くなった」は「速い」ではない。** 上限を決めたら、直したあとに
  もう一度その上限で測る。そして、**往復が高い環境では「必ず空振りする処理」も
  待ち時間になる**（ローカルでは0秒に見えるので気づけない）。

## 2026-08-06 自分で足したCIジョブが auto-merge を永久に止める形になっていた

- **何が起きたか**：本番の3タップ経路を日次で踏むジョブを `prod-smoke.yml`（`push` で走る）に
  足し、`if: github.event_name != 'push'` で push のときだけ飛ばすようにした。
  結果、**push のたびにそのジョブが `skipped` になる**。
- **`auto-merge.yml` は「`skipped` を緑扱いしない」**（必須 check を skip させて素通りさせる
  裏口を塞ぐため。規則そのものは正しい）。したがって PR #45 も、**それ以降のすべてのPRも
  永久にマージされない**状態になっていた。CI は全部緑なのに `mergeable_state` が `unstable`
  のまま動かないので気づいた。
- **なぜやったか**：「push では走らせたくない」という要件に対して、
  **ワークフローの置き場所ではなく `if:` で解こうとした。** `push` で走るワークフローに
  条件付きジョブを足すと skipped が生まれる、という結合を見ていなかった。
- **どう直したか**：**検査は緩めない。** ジョブを別ファイル
  `.github/workflows/prod-demo-check.yml`（`schedule` + `workflow_dispatch` のみ）に移した。
  push のイベントでは check run 自体が生まれないので、skipped も起きない。
  そもそも「本番が今どうなっているか」はコミットごとに見るものではない。
- **教訓**：**push で走るワークフローに `if:` でジョブを飛ばさない。** 飛ばしたいなら
  ワークフローを分ける。緑を通すために auto-merge 側の判定を緩めるのは、
  裏口を作るのと同じ（`AGENTS.md`「完了の証明」）。

## 2026-08-06 auto-merge が最後のチェック完了イベントを取りこぼした（PR #45）

- **何が起きたか**：PR #45 の CI が 10:51:13 に全部緑になったのに、`auto-merge.yml` の
  最後の実行は 10:49:39 だった。**最終状態を見る実行が1度も走っていない。**
  条件（全緑・非draft・hold 無し・mergeable）は満たしていたので、手でマージした。
- **なぜ起きたか**（推定）：`auto-merge.yml` は `check_suite: completed` と `status` で
  発火し、`concurrency: { group: auto-merge, cancel-in-progress: false }` で直列化される。
  複数のワークフローがほぼ同時に終わると、後続の発火が先行実行の待ち行列に入り、
  途中の実行が cancelled になる（実際に 10:48 台に cancelled が2件ある）。
  **最後の1件が落ちると、誰も最終状態を評価しない。**
- **直していない。** 今回は手でマージした。塞ぐなら「定期実行で open PR を評価し直す」か
  「`cancel-in-progress: true` にして常に最後の1回だけ走らせる」。
  後者のほうが素直だが、マージ中の実行が切られる危険があるので、決める前に
  auto-merge のマージ処理が中断に耐えるかを確かめること。
- **教訓**：**イベント駆動の自動化は、イベントを取りこぼした時に何も言わない。**
  「動いていない」ではなく「静かに何もしない」形で失敗するので、
  待っているだけでは気づけない。止まっていることに気づく手段を別に持つ。

## 2026-08-06 デモの入口が「押しても無反応」だった（Server Action のIDはビルドごとに変わる）

- **何が起きたか**：利用者から「デモを触るボタンを押しても無反応」。
  シークレットウィンドウでは動く。**ページを取り直すと直る＝キャッシュの問題。**
- **原因**：トップの入口が `<form action={startDemoAction}>`（Server Action）だった。
  **Server Action の呼び出し先IDはビルドのたびに変わる。** ブラウザが持っている
  古いページから押すと、新しい本番にはもう存在しないIDへ送るので何も起きない。
  **デプロイのたびに再発する。** 商談の直前にデプロイしたら、その場で起きる。
- **なぜ検査から漏れたか**：E2E も `scripts/prod-demo-check.sh` も、**毎回ページを
  取り直してから**押していた。取り直せば必ず新しいIDになるので、この失敗は原理的に
  再現しない。「古いページを開いたまま新しい本番へ送る」という状態を作っていなかった。
- **どう直したか**：入口を**素のフォームの POST**（`/demo/start` のルートハンドラ）にした。
  **URL はビルドで変わらない。** 古いページからでも届く。JavaScript が動かない環境でも同じ。
  - ルートハンドラには Server Action の同一オリジン判定が無いので、`Origin` を自前で見る。
    見ないと、他所のサイトのフォームから叩かれてセッション Cookie を貼り替えられる
    （ログイン中の利用者なら実質その場でログアウト）。E2E で 403 を確かめている。
  - `scripts/prod-demo-check.sh` も、HTMLからIDを拾う形をやめて固定URLを叩く形にした。
    **検査がビルドの中身に依存しなくなった。**
- **教訓**：**「画面を開き直せば直る」は、直っていない。** 利用者は開き直さない。
  ビルドごとに変わる識別子を、利用者の手元に残るページに埋めない。

## 2026-08-06 デモの中身を直しても、既にデモ中の人には古いものが返り続けていた

- **何が起きたか**：利用者から「見た感じ変わってないぞ」。デモは
  「既にデモ中の訪問者は作り直さず、自分の案件へ戻す」（連打で書き込みが増えないため）。
  この分岐が、**中身を直したことを見ていなかった**。デモの有効期間は6時間あるので、
  **直した当日に触った人ほど古いものを見る**。商談前に自分で確認した人ほど当たる。
- **どう直したか**：デモの中身の版を Cookie に持たせ、版が違えば
  **同じ識別子のまま作り直す**（新しい識別子にすると古い案件が期限まで残る）。
  版は **`lib/demoFixture.ts` の中身から算出する**（`lib/demoVersion.ts`）。
  **手で上げる形にしない。** このリポジトリの失敗の多くが「規則はあったが機構が無い」
  形をしており、「fixture を直したら版も上げる」は必ず忘れる。
  E2E で「版を古い値に差し替えたら別の案件が返る／同じ版なら同じ案件が返る」を見ている。
- **教訓**：**キャッシュや再利用の分岐を入れるときは、「何が変わったら捨てるのか」を
  同時に決める。** 決めていないものは、変えたときに黙って古いままになる。

## 2026-08-06 版の材料に含め漏れがあった（名前を並べる形にしたのが原因）

- **何が起きたか**：デモの中身の版（`lib/demoVersion.ts`）を、fixture の値を
  **名前で並べて**作っていた。`DEMO_WORK_NAME` が漏れていた（CodeRabbit の指摘）。
  漏れた値だけを直しても版が変わらず、**古い案件が返り続ける**＝
  この仕組みを入れる前と同じ状態に戻る。
- **なぜ起きたか**：「`seedDemoData` が書き込む値をすべて含めること」とコメントに
  書いて、人が守る前提にした。**このリポジトリの失敗の多くが同じ形**で、
  版を手で上げる形を避けたのに、材料を手で並べる形で同じ穴を作っていた。
- **どう直したか**：`import * as` で **fixture の export 全部**を材料にした。
  export を足した時点で自動的に入る。あわせて
  `tests/demoVersion.test.ts` で「**どの export を落としても版が変わる**」を見る。
  指摘どおり `DEMO_WORK_NAME` を落とした状態を作って、赤くなることを確かめた。
- **教訓**：**「全部入れること」と書いた時点で、それは機構ではない。**
  列挙を人に任せず、列挙そのものを機械から取る。

## 2026-08-06 squash マージ後に同じブランチへ積み足した（4回目）

- **何が起きたか**：PR #46 が squash マージされたあと、**ブランチを作り直さずに**
  次の変更（#47）を積んだ。squash は main に**別のコミット**を作るので、
  同じ内容でも履歴が分かれ、`docs/failures.md` と `docs/handoff.md` が競合した。
  CodeRabbit の「Resolve merge conflicts」で気づいた。
- **同じ失敗が `docs/failures.md` 2026-08-03 / 2026-08-04 にもある。今回で4回目。**
  `AGENTS.md`「実装の進め方」に
  「マージ完了の通知を受けたら、次の変更に着手する前に必ず
  `git fetch origin main && git checkout -B <branch> origin/main`」と**書いてある**。
- **なぜまた起きたか**：**機械で止めていない。** 規則は3回書き足されたが、
  「マージ済みのブランチに積んだら落とす」検査が無い。今回も、
  利用者の不具合報告に飛びついて、着手前の作り直しを飛ばした。
  **急いでいるときほど飛ばす手順は、機構にしないと守れない。**
- **どう直したか**：`git checkout -B <branch> origin/main` で作り直し、
  2つのコミットを cherry-pick し直した（競合なしで載った）。
- **教訓**：この項目が4つ目である事実そのものが、
  「規則を書き足す」では止まらないことの証拠になっている。
  次に同じことが起きたら、規則をもう1行増やすのではなく検査を作る。

## 2026-08-06 手元のテストが1件だけ落ちた（原因はDBの蓄積。ただし本物の穴も見つけた）

- **何が起きたか**：`tests/subcontractors.test.ts` の
  「一覧には自分の下請だけが出る」が、手元で3回連続で落ちた。CI は緑。
- **原因**：**手元の Supabase を作り直していなかった。** 同じ owner_id で何度も
  試験を回した結果、その所有者の下請が **1419行**まで積み上がっていた。
  一覧が実際に返したのは **1000行**。`supabase db reset` で直った。
- **ここで見つけた本物の穴（直していない）**：1000 は PostgREST（Supabase）の
  既定の上限で、**アプリ側では何も指定していない**。`listSubcontractorsForOwner` に
  限らず、`lib/db/` の一覧系はどれも**上限を超えた分を黙って落とす**。
  例外も警告も出ないので、利用者からは「登録したのに出てこない」に見える。
  実務のリフォーム元請が下請を1000社持つことは無いが、**案件は年数で超えうる**。
  対処するなら、明示的に範囲を指定して「まだ続きがある」ことを呼び出し側へ返す形にする。
  今回のPRの範囲外なので入れていない。
- **教訓**：**手元だけ落ちる検査は、まず環境を疑う。** そして、環境を疑って終わりに
  しない。今回は「1000で切られる」という、本番でも起きうる挙動がその陰にあった。

## 2026-08-06 デモを一度見せると、トップからデモに戻れなくなっていた

- **何が起きたか**：利用者から「デモを触るボタンが出ない」。画面には
  案件／下請台帳／会社設定とログアウトが並んでいた。
- **原因**：**デモの利用者もログイン扱いになる**（使い捨ての識別子でセッションを持つ）。
  トップ画面は `loggedIn` だけを見て出し分けており、**ログイン中はデモの入口を出さない**
  作りだった（「実データの画面にデモを混ぜない」という意図は正しいが、
  デモの利用者まで巻き込んでいた）。デモの有効期間は6時間あるので、
  **商談で一度見せたら、その日はトップから見せ直せない。**
- **私の診断が最初は間違っていた。** ブラウザのキャッシュだと言って、
  再読み込みを頼んだ。実際には再読み込みしても直らない。
  **画面のスクリーンショットを見て初めて分かった**（案件一覧が出ている＝ログイン扱い）。
  症状を聞いた時点で本番を「デモ中のCookieで」取り直せば、その場で分かった。
- **そして、この症状は一度見ていた。** 同じ日に E2E を書いていて
  「デモ中だとトップのボタンが押せない」に当たり、**検査の書き方の問題として回避した**
  （`context.request.post` に切り替えた）。**利用者の不具合そのものだったのに、
  自分の都合の悪さとしてよけた。**
- **どう直したか**：デモの利用者には「デモの続きへ」を出す（同じ `/demo/start` を押す）。
  実利用者には出さない。`tests/home.test.tsx` と
  `e2e/demo-three-taps.spec.ts`「デモを始めたあとも、トップからデモへ戻れる」で見る。
  不具合を再現した状態（`demoVisitor` を常に false）で、この E2E が落ちることを確かめた。
- **教訓**：**検査を書いていて「思ったように操作できない」と感じたら、それは利用者も
  同じところで詰まっている可能性がある。** 回避策を書く前に、なぜ操作できないのかを
  製品の側から見る。

## 2026-08-07 トップ画面をログイン状態で出し分けていたのをやめた（利用者の指示）

- **利用者からの指示**：「ログインしてようがしてなかろうが毎回同じトップ画面から
  始まるようにしろ。URLに飛ぶ→トップページにデモ操作のボタンと右上に開発者用の
  小さい黒丸ボタンがあるのみにしろ」
- **なぜこの指示が出たか**：前日、デモを一度触ると（＝デモのセッションを持つと）
  トップからデモの入口が消えて、商談で見せ直せなかった。
  こちらは「デモ中の人にも入口を出す」という**条件分岐を1本足す**直し方を出したが、
  利用者の判断は**出し分けそのものをやめる**だった。
- **こちらの直し方が悪かった理由**：条件を足す形は、条件の数だけ「出ない組み合わせ」が
  増える。今回の事故はまさに「ログイン扱いだが実はデモの人」という、
  想定していなかった組み合わせで起きた。**分岐を足しても、次の組み合わせで同じことが起きる。**
  出し分けを無くせば、その種類の事故は構造的に消える。
- **どう直したか**：トップに出すのは**デモのボタン1つだけ**。
  案件一覧・下請台帳・会社設定・ログイン／ログアウトは、右上の小さな丸（`<details>`）の
  中にしまった。`tests/home.test.tsx` は**ログインの2状態を両方**流して、
  表に出るものが同じであることを見る。
- **あわせて社名を「業者A / 業者B / 業者C」にした**（同じ指示の中の別項目）。
  それらしい架空の社名（サンプル内装工業 等）は読む手間が増え、
  比較表の見どころ（同じ工事で単価が違う）から目が逸れる、という指摘。
  検査側に社名を直書きしていた箇所は `lib/demoFixture.ts` 参照に変えた。
- **教訓**：**「状態によって出し分ける」は、状態が増えるたびに事故の面が広がる。**
  出し分けを足す前に、出し分けそのものが要るかを疑う。

## 2026-08-07 比較表の採用ボタンが読み取れなかった（文字で出し分けていた）

- **利用者からの指摘**：「採用する／採用をやめる／は分かりにくい。採用という表示のみで
  良い。3つあるうちの採用している業者のボタンのみ青くしろ、不採用の業者は灰色にしろ。
  それでわかる」「採用中：業者B とかの表示は不要」
- **何が悪かったか**：3社ぶんのボタンが縦に並ぶのに、**ボタンごとに文字が違った**
  （採用する／採用をやめる）。読み手は3つの文字列を読み比べて、今どれが採用中かを
  推測することになる。**状態は色で見せて、文字は揃えるほうが速い。**
  さらに下に「採用中: 業者B」を別途出していて、同じことを2回言っていた。
- **どう直したか**：ボタンは3社とも「採用」。採用中だけ濃い青（白抜き）、
  それ以外は灰色。押すと入れ替わる（採用中を押すと外れる）。
  「採用中: 社名」の行は消した。
  **色だけに頼らない**ため `aria-pressed` を付けた（読み上げにも状態が届く。
  検査もこの属性で見るので、色の指定を変えても検査は壊れない）。
- **あわせて工事名を帯にした**：工事名と会社名がどちらも太字で縦に並んでいて、
  どちらがどちらか分からないという指摘。濃い地に白抜きの帯にし、
  上に小さく「工事項目」と置いた。
- **教訓**：**同じ種類の選択肢が並ぶところで、選択肢ごとに文字を変えない。**
  文字は揃えて、状態は見た目（と機械が読める属性）で示す。

## 2026-08-07 デモの1タップ目が遅かった本当の原因はリージョンだった

- **経緯**：デモの1タップ目は、DBへの往復を 20回 → 12回 → 11回、直列の波を 20 → 6 → 5 と
  削ってきた。それでも本番では 2.3〜3.9秒で、上限 3.5秒の境目に張り付いたままだった。
- **切り分け**（本番の実測）：DBに触らない画面が 0.26秒、1往復する画面が 0.72〜1.13秒。
  **往復1回あたり 0.45〜0.85秒。** Vercel の関数は `iad1`（米国東部）で動いていた。
  `0.26 + 5波 × 0.5 ≒ 2.8秒` で実測と一致する。**残りは全部、距離だった。**
- **どう直したか**：`apps/web/vercel.json` に `"regions": ["hnd1"]`（東京）を足した。
  Supabase は `ap-northeast-1`（東京）。**コードは1行も変えていない。置き場所を変えただけ。**
- **反省点は「気づくのが遅かった」ことではなく、順番**：
  往復の回数を3回に分けて削る間、**1回も「そもそも1往復がなぜ0.5秒かかるのか」を
  測っていなかった。** 最初に1往復の実測を取っていれば、削る前に距離だと分かった。
  **遅いときは、まず1回ぶんのコストを測る。** 回数を減らすのはその後。
- **リージョンは利用者にしか分からなかった**（Supabase の管理画面の値）。
  エージェント側で調べようがない情報は、早めに聞く。最後まで聞かずに削り続けた。
- **結合が増えた**：`vercel.json` の `regions` と Supabase のリージョンは対になる。
  JSON にコメントを書けないので、`AGENTS.md`「値が結合している箇所」に理由を書いた。
  外れたことは `scripts/prod-demo-check.sh` の待ち時間の上限が検知する。

## 2026-08-07 このセッション全体の総括（同じ形の失敗が5回出た）

デモを商談で見せられる状態にするまでに、利用者から5回「動かない／分からない」と言われた。
**5回とも、自動検査は緑だった。** 個別の原因は各項目に書いたが、形は同じだった。

1. **見積書が全行0円**（検査はPDFが出ることしか見ていなかった）
2. **入口を押しても無反応**（検査は毎回ページを取り直してから押していた）
3. **見た目が変わらない**（既にデモ中の人に古い案件を返し続けていた）
4. **デモのボタンが出ない**（デモ中はログイン扱いになり、入口が消えていた）
5. **採用ボタンと工事名が読み取れない**（機械には読めるが、人には読み取れない）

**共通しているのは「検査が、利用者の立っている場所に立っていない」こと。**
毎回まっさらな状態から始める検査は、**利用者が既に何かを持っている状態**を再現しない
（古いページ、古いCookie、進行中のセッション）。1〜4はすべてその形をしている。
5は、機械が読める（DOMにある）ことと人が読み取れることの差。

**次にやること**：新しい経路を作るときは、
「まっさらな人」だけでなく**「一度触った人」でも一度踏む**。
それができない検査は、利用者の報告でしか壊れが分からない。

## 2026-08-07 リージョンを東京に寄せた結果（上の項目の結末）

- 上の「デモの1タップ目が遅かった本当の原因はリージョンだった」の続き。**効いた。**
  本番反映後（`bda1d05`）の実測：
  デモ開始 **2.188 / 0.747 / 1.051 秒**（上限 3.5s）、比較表 **1.113 秒**（上限 3.0s）。
  `scripts/prod-demo-check.sh` は全項目 OK。
- **効いたことの直接の証拠**：DBに触らない画面（0.56秒）とDBに1往復する画面（0.54秒）に
  差が無くなった。変更前は 0.26秒 と 0.72〜1.13秒 で、その差が往復のコストだった。
- **注意**：この計測点は日本ではないので、上の数字には計測元から東京までの通信時間が
  丸ごと乗っている。**日本から使う商談では、これよりさらに速い。**
  逆に言うと、`scripts/prod-demo-check.sh` を海外の実行環境から回すかぎり、
  この上限は「日本の利用者の体感」より厳しい側に出る。上限を緩めない理由がここにある。

## 2026-08-07 フローを「実装があるかどうか」で書いた

`docs/flows.md` の16工程に【デモ】【サンプル】【自動】の印を付けたとき、
**実務の流れではなく、いまの実装の形をそのまま工程として書いた。**

- **工程11「下請けが見積もりの確認をする」を「（印なし）画面が無い・データも無い・
  処理も無い」と書いた。** 確認の工程は実務に存在する。無いのは実装であって工程ではない。
- **工程9「書類が届く」に、届いたことの連絡（「見積依頼が届きました」）を書かなかった。**
  同じく工程13の元請側にも「見積もりが届きました」を書かなかった。
  どちらも実務では通知か画面がある。メール送信が未実装だという理由で、
  **工程そのものを薄く書いた。**
- **「だから【自動】は付かない」という実装の言い訳を、工程一覧の中に混ぜて書いた。**
  工程の説明を読みに来た人が、実装の都合を読まされる形になっていた。

**利用者の指摘**：「通常は見積もりが届きましたとかそういう画面なり通知が来るだろ。
実装してないからありません、ではない。」

**直し方**：
- 工程一覧は**実務の流れをそのまま**書く。実装の有無で工程を削らない・薄く書かない。
- 足りない実装は**別の表**（「いまの実装が、上のとおりになっていない箇所」）に集める。
  3件だったものが、通知2件を足して5件になった。**数が増えたのは、これまで
  工程の側を削って辻褄を合わせていたから。**
- 「デモでは走らせていない処理」も工程一覧から出して別表にした。

**この失敗の形**：`docs/flows.md` は「実装から起こした記録」だと自分で冒頭に書いていた。
そこから、**実装に無いものは工程にも無い**という書き方に滑った。
`AGENTS.md`「業務ドメインを決めるときの作法」6 が言っている
「成文化されていない前提は調査では取れない」と同じ場所で転んでいる——
**実務の流れは実装からは起こせない。**

## 2026-08-07 秘密情報の形をした値が、経路に3回出た

**3件とも運用の値ではない。事実として残す。**

1. **サブエージェントがビルドを試したとき、`AUTH_SECRET=<プレースホルダ文字列>` を
   シェルに書いた。** 実在の鍵ではなく、どこにも登録していない。なお、その変数は
   ビルドに不要だった（再実行で確認済み）。
2. **`pnpm exec supabase status` の出力に、ローカル Supabase の anon キーが出た**（私）。
3. **同じことをサブエージェントも1度やった**（service_role キーを含む）。以後は
   `status -o env` をファイルへ落として `source` する形にして、表示していない。

**露出と影響を分けて書く。** 2・3 は Docker 上のローカルインスタンスが**毎回同じ値で
発行する既定値**で、`127.0.0.1` に対してのみ有効。1 は作り話の文字列。
**どれも本番に登録されていないので、認証への影響は無い。** 運用の鍵は生成も受領も
していない。

**直し方**：`supabase status` を素で叩かない。`-o env` を使い、出力はファイルへ落として
`source` する（`scripts/smoke.sh` が既にそうしている）。サブエージェントへの指示に、
この作法を書き添える。

## 2026-08-07 デモの経路を作り直したとき、検査の側が実装より先に間違っていた

デモを3タップから D1〜D9 の並びに作り直し、E2E を新しい並びで書き直したところ、
**2件落ちた。原因は実装ではなく、私が書いた検査のほうだった。**

- D3 の「保存」「修正」、D5 の「見積もりを見る」を、**すべて `button` で探していた。**
  実装はどれも `Link`（画面を移るだけの操作はリンクが正しい）。
- **落ちた時点で「実装を直す」と言いかけた。** `AGENTS.md`「落ちたら実装を直す。
  検査のほうを緩めない」を、**検査が間違っている場合にも当てはめかけた**という失敗。

**区別**：検査を**緩める**（判定を消す・上限を上げる）のは禁止。検査が**事実と違う**
（役割の取り違え）のを直すのは、緩めることではない。**どちらかを見分ける手は
「直したあとも、壊れたときに落ちるか」**。今回は3つの操作がそろっていることを
引き続き見ているので、落ちる条件は変わっていない。

**教訓**：画面を先に見ずに検査を書くと、実装が正しくても落ちる。
新しい画面の検査は、**要素の役割を実装で確かめてから**書く。

## 2026-08-07 表に経路が無いことに気づかないまま実装を進め、検査が表の外へ出た

デモの経路を D1〜D9 に作り直したとき、**表に D7 → D9 の操作が無かった。**
D7（下請け見積もり一覧）から見積書PDFへ進む手段が表に書かれていないのに、
**そこに気づかないまま実装を進めた。**

その結果どうなったか。E2E は D7 まで表のとおりに進んだあと、
**`page.goto` で `/projects/<id>/comparison`（表に無い画面）へ飛んで**
そこの「見積書PDFを出力」を押していた。`scripts/prod-demo-check.sh` も同じ形で、
比較表の合計を「D9 の代用」と書いて済ませていた。
**利用者の経路が途切れているぶんを、検査が自分で迂回路を作って埋めていた。**

レビュー（CodeRabbit）がこれを指摘し、利用者に確認したところ
**「D7 に『見積書を出す』を1つ置く」**ことになり、`docs/flows.md` の表に行が足された。
検査もその経路に直した（比較表を開くのは金額の確認だけになり、
**なぜ表に無い画面を開くのか**をコメントに明記した）。

**教訓**：表を正とすると決めたなら、**実装を始める前に表を最後まで読んで、
経路が途切れていないかを確かめる。** 途切れているのに実装を進めると、
実装は表のとおりに見えるのに、**検査の側が表の外へ出て辻褄を合わせる**ことになる。
そして検査は「通っている」と言い続けるので、途切れていること自体が見えなくなる。

## 2026-08-07 ローカル Supabase の既定キーが、また経路に出た（今度は `supabase start` から）

D7 のボタンを足す作業でローカル Supabase を起動したとき、
**`pnpm exec supabase start` の出力そのものに ANON_KEY / SERVICE_ROLE_KEY 等が JSON で
並んで出た**（私。ツール出力に2回）。1回は手で叩いたとき、もう1回は
`bash scripts/e2e.sh` の中から。

**露出と影響を分ける。** 出たのは Docker 上のローカルインスタンスが**毎回同じ値で発行する
既定値**で、`127.0.0.1` に対してのみ有効。本番には登録していないので**認証への影響は無い**。
運用の鍵は生成も受領もしていない。**それでも経路には出たので、事実として残す。**

**同項目 2026-08-07「秘密情報の形をした値が、経路に3回出た」の直し方が届いていなかった。**
あのときの結論は「`supabase status` を素で叩かない。`-o env` を使う」で、
`scripts/smoke.sh` / `scripts/e2e.sh` はそのとおりになっている。
**出所が違った**——出しているのは `status` ではなく `start` 自身で、
この CLI の版は起動時に同じ env の JSON を標準出力へ書く。両スクリプトとも
`start` は素で呼んでいるため、コンテナが停止していた回は必ずこれが出る。

**次にやること（未着手）**：`supabase start` の出力もファイルへ落とし、
失敗したときだけ中身を出す形にする。**「status だけ塞げば足りる」と思い込んでいた**のが
今回の抜けで、塞ぐ対象をコマンド名ではなく「秘密情報の形をした値を出す経路」で
数え直す必要がある。

## 2026-08-07 書きかけの状態を混ぜてコミットした

複数の担当が**同じ作業ツリー**で並行して動いている最中に、片方の担当ぶんだけを
コミットするつもりで `git add -A` を打ち、**他の担当が書きかけだったファイルまで
1つのコミットに入れた**（`e8a8839`）。そのときテストは通っていたが、
**中身を確かめずに混ぜた**。「通っているから問題ない」は、混ざっていないことの
証明にならない（書きかけの側がまだ検査を持っていないだけかもしれない）。

**根因**：`git add -A` は作業ツリーの状態をそのまま取る。同じツリーで並行して
動いていれば、他人の途中経過も等しく入る。**コミットの単位を決めているのが
「自分が何を直したか」ではなく「いまツリーに何があるか」になっていた。**

**直し方**：並行して動かしているときは、**終わった担当の報告がそろうまで
コミットしない。** どうしても分けたいなら、`git add -A` ではなく
**ファイルを指定して add する**（何を入れたかを自分で言えない add をしない）。

## 2026-08-07 同じローカル Supabase に対してテストを同時に走らせると、無関係なテストが30〜47件落ちる

`pnpm --filter web test` を2つ同時に走らせたところ、**互いに無関係なテストが
30〜47件落ちた**。原因は案件の登録件数で、2周ぶんの案件が同じDBに積もり、
この PR で入れた**1オーナーあたり100件の上限**（`lib/limits.ts` の
`MAX_PROJECTS_PER_OWNER`）に当たる。`tests/globalSetup.ts` は**実行開始時に1回しか
消さない**ので、後から始まったほうの掃除が先に始まっていたほうの積み上げを消さず、
どちらも上限に届く。

**実装の欠陥ではない。** 上限は仕様どおりに効いていて、落ちているのは
「同じDBを2つの実行が共有している」という走らせ方のほう。

**いまは塞いでいない。** ローカルでも CI でも並走させていないため、この構成では
再現しない。**ただし CI でテストジョブを並走させると同じことが起きる**
（1つのローカル Supabase を共有するなら、実行ごとに owner_id を分けるか、
実行ごとにDBを分ける必要がある）。**塞いでいないことをここに書いておく**——
並走を足す人が、この落ち方を「自分の変更のせい」と読み違えないため。

## 2026-08-07 `supabase start` の既定キーが、また経路に出た（同項目の再発。塞いでいないため）

検査を D1〜D10 に合わせて直す作業で `bash scripts/e2e.sh` を走らせたところ、
**同スクリプトの中の `pnpm exec supabase start` が、ローカル Supabase の既定値
（ANON_KEY / SERVICE_ROLE_KEY 等）を JSON でツール出力に書いた**（私。1回）。

**露出と影響を分ける。** 出たのは Docker 上のローカルインスタンスが毎回同じ値で発行する
**既定値**で、`127.0.0.1` に対してのみ有効。本番には登録していないので**認証への影響は
無い**。運用の鍵は生成も受領もしていない。**それでも経路には出たので、事実として残す。**

同日の項目「ローカル Supabase の既定キーが、また経路に出た（今度は `supabase start` から）」
で挙げた**「次にやること（未着手）」がまだ未着手のまま**なので、`scripts/e2e.sh` /
`scripts/smoke.sh` を走らせるたびに（コンテナが停止していた回は必ず）これが出る。
自分で叩く `supabase status` は `-o env` でファイルへ落として塞いでいるが、
**スクリプトの中の `start` は塞いでいない。** 今回もそこから出た。

## 2026-08-07 実装側に古い画面番号のコメントが残った

D2「案件をつくる」を足して番号を D1〜D10 に振り直したとき、**検査と文書は直したが、
実装のコメントに古い番号が残った**（`app/drafts/page.tsx` の「D8 下書き保存フォルダ」→
正しくは D9、`components/QuoteDocumentLines.tsx` の「D6 見積もり書類」→ 正しくは D8、
`lib/flowText.ts` と `lib/content.ts` の「D2〜D9 の文言」→ 正しくは D2〜D10）。

**動きには影響しないが、次に読む人が古い表を信じる。** 番号は
`docs/flows.md`「デモの画面の並び」が正で、そこを変えたら**実装のコメントも同時に直す**。

**なぜ残ったか**：番号を振り直した作業と、画面を作る作業を別々の担当に分けていて、
**どちらの担当も「コメントの番号」を自分の担当だと思わなかった**。
表を変えたときは、`rg 'D[0-9]'` で全部拾ってから配る。

## 2026-08-07 レビューがマージ後に届き、直しが本番へ1回遅れた

PR #54 で `@coderabbitai review` を draft のまま呼んだところ、返ってきたのは
「指定した4箇所にブロッカーは無い」という**部分レビュー**だった。それを緑と読んで
draft を解除したが、**解除がきっかけで全体レビューが走り、実質12件の指摘が届いた**。
そのときには auto-merge が既にマージを済ませていた。

**部分レビューは全体レビューの代わりにならない。** `@coderabbitai review` に
見てほしい箇所を列挙して呼ぶと、その範囲だけを見て返す。全体は draft 解除で走る。
**次からは、draft を解除したあとに全体レビューの結果が出るまでマージを待つ**
（auto-merge を有効にしたまま解除すると待てないので、解除の前に指摘を出し切らせる
必要がある。今回は解除と同時にマージ条件が揃ってしまった）。

届いた12件のうち、実害のあったものは3つ。**どれも「動くかどうか」ではなく
「出した情報が正しいか」の側**だった。

1. **実在の下請の見積書に、架空の住所・電話・担当・支払条件・見積番号が印字されていた。**
   デモ3社ぶんの決め打ち（`DEFAULT_QUOTE_COVER`）を、**メールが一致しない社すべてに
   既定値として当てていた**。デモ用の値を「無いときの穴埋め」に流用したのが誤り。
   DBに欄が無いものは埋めずに「未入力」と出す。既定値そのものを消した。
2. **国交省の様式例の文を原文のまま載せていて、出典を配布物に出していなかった。**
   出典はコードのコメントにだけ書いてあった。**コメントは配布物に付いてこない。**
   利用の条件（公共データ利用規約 第1.0版）は出典の記載なので、画面に出す。
3. **やり直しで写真の実体を消せなかったとき、消す手がかりごと消していた。**
   同じ識別子で作り直すと `photos` の行（`storage_path`）が案件ごと消え、
   商談で撮った現場写真が誰にも辿れないまま Storage に残る。消せなかったときは
   前の識別子を残し、**新しい識別子**でデモを作り直す。

**共通していること**：3つとも「デモだから」で通していた箇所が、デモの外に漏れていた。
デモ用の値・デモ用の手抜きは、**デモの外に出る経路が無いことを確かめてから**置く。

## 2026-08-07 auto-merge を「切った」つもりで切れておらず、2回続けてレビュー前にマージされた

PR #55 でも、全体レビューが返る前にマージされた。前項（同日「レビューがマージ後に届き…」）で
決めた「解除のあとに全体レビューを待つ」を実行するため、GitHub API の
`disable_pr_auto_merge` を呼んで待つ構えを取っていたが、**その API はこのリポジトリの
マージには関係が無かった。**

`.github/workflows/auto-merge.yml` は、GitHub 標準の auto-merge 機能を使っていない。
**緑を確認して `gh pr merge --squash` を直接叩くワークフロー**で、無料プランでも動かすために
そう作られている（ファイル冒頭にその旨が書いてある）。したがって
`disable_pr_auto_merge` は何も止めない。**止まる条件はワークフロー自身が見ている
2つだけ**——`draft` であること、`hold` ラベルが付いていること。

**「止めた」と言う前に、何が動かしているのかを読む。** 今回は仕組みを読まずに、
名前が一致する API を呼んで止めたつもりになった。ファイルは
リポジトリの中にあり、読めば5分で分かる状態だった。

同時に、前項に書いた対策そのものも誤りだったと分かった。**「解除の前に指摘を出し切らせる」は
できない**——CodeRabbit は draft のあいだ全体レビューを走らせない設計なので、
全体レビューは解除しないと始まらない。**正しい手順は、解除する前に `hold` ラベルを付け、
全体レビューの指摘を片付けてから `hold` を外すこと。**

もう1つ分かったこと：**CodeRabbit はレート制限でレビューを走らせなかったときも、
コミットステータスとしては `success` を返す**（説明文だけが `Review rate limited`）。
ワークフローは状態の色しか見ないので、これを緑として通す。
**レビューが実際に行われたかどうかは、ステータスの色では判定できない。**

なお、この記録の前項にある表現（`それを緑と読んだ` 等）について、
中立的な言い換えの指摘を受けたが、**既存行は書き換えていない**。
このログは append-only で、訂正も追記で行う決まりのため
（`AGENTS.md`「完了の証明」）。以降の記述では中立的な表現を使う。

## 2026-08-08 画面の名前を変えて、本番の日次検査を赤にしたまま気づかなかった

利用者の指示で D9 の見出しを「下書き保存フォルダ」→「保存フォルダ」に変えたとき
（PR #57）、`scripts/prod-demo-check.sh` がその文字列を本文から探していることに
気づかず、片方だけを直した。**日次の本番検査（`.github/workflows/prod-demo-check.yml`）は
それ以降ずっと `FAIL D9 見出しが無い` で落ちていた。**

**気づけなかった理由は2つあり、どちらも自分の判断である。**

1. **利用者の指示で機械検査を省いていた**（「検査不要」「機械チェックなしでデプロイしろ」）。
   これは利用者の判断で、従うこと自体は正しい。ただし**省いてよいのは自分の手元の検査**で
   あって、**本番を毎日踏んでいる検査が赤になった事実まで見なくてよい**という意味ではない。
   出したあとに一度も状態を見に行かなかった。
2. **文言が検査のマーカーを兼ねている結合を、変える前に確かめなかった。** `AGENTS.md` には
   同じ形の結合（`HOME_HEADING`、`DEMO_ENTRY_TEXT.start`）が既に2件書いてある。
   **書いてある注意を、3件目に当てはめなかった。** 文言を変えるときは
   `rg "<変える前の文字列>"` でリポジトリ全体を引く。

さらに悪いのは、**この赤に自分で気づかず、商談資料の事実確認をサブエージェントに
投げて初めて分かった**こと。資料には「毎日自動で測っています。遅くなれば自動で分かります」
と書いてあり、**その検査が落ちている状態で商談に出すところだった**。

**「自動で分かる」と書いた仕組みは、緑であることを自分で確かめてから話す。**
仕掛けがあることと、それが動いていることは別。

対処：`scripts/prod-demo-check.sh` の見出しを実装に合わせ（検査を緩めたのではなく、
画面の名前の変更に追随させた）、この結合を `AGENTS.md` に3件目として明記した。

## 2026-08-12 リポジトリ取り込み後に handoff を読まず、利用者に指摘された

指定リポジトリを取り込んだ直後、`AGENTS.md` と `docs/handoff.md` を読む前に
「チェックイン対象の変更はない」とだけ返した。`AGENTS.md` には、実装の前に
`docs/design.md` を読むこと、セッション状態は `docs/handoff.md` にあることが明記されている。
今回の `docs/handoff.md` には、KYOZAI系Skillの正本化、次回やること、削除してよい出力と
削除してはいけない出力の区別が書かれていた。

**リポジトリを pull / clone / fetch した直後は、状態確認だけの依頼に見えても
`AGENTS.md` と `docs/handoff.md` を先に読む。** 作業ツリーがクリーンかどうかは
git の状態で分かるが、次に何を守るべきかは引継ぎにある。

## 2026-08-12 Vercelへのmonorepo配備で、ローカル前提の設定を順番に露出させた

公開体験版の初回配備では、アプリ単体配備にlockfileが無い、ルート配備ではNext.jsを
検出できない、アプリ単体ではルートの`tsconfig.base.json`を参照できない、という失敗が
順に発生した。いずれも、ローカルmonorepoでは存在する親ディレクトリとworkspace情報が、
Vercelへ送る配備単位にも存在すると仮定したことが原因だった。

対処として、`apps/web`を単独で配備できるように`tsconfig`を自己完結させ、Vercel用の
install/build設定を`apps/web/vercel.json`へ置いた。Next.jsのTurbopack rootも、ローカルと
Vercelで明示的に切り替えた。

**配備単位を決めたら、そのディレクトリだけを送ってbuildできるか先に確認する。**
monorepoのローカルbuild成功は、サブディレクトリ単体配備の証明にならない。

## 2026-08-12 Windows上のpnpm 10.33.0が依存リンク作成中に異常終了した

`corepack pnpm@10.33.0 install`は、package取得後のリンク作成中にWindows例外
`-1073740791`で終了し、`apps/web/node_modules/.bin`が作られなかった。表示上は
通常の進捗で止まり、PowerShellからは原因行が出なかった。pnpm 11.16.0で同じlockfileを
installすると復旧し、その後の型検査、Lint、単体テスト、build、E2Eは成功した。

VercelのLinux buildではpnpm 10.33.0が成功しているため、現時点ではWindowsローカル固有の
互換問題として扱う。**installが短時間で終了しても、`.bin`と実コマンドの存在まで確認する。**

## 2026-08-12 OpenAIの途中終了JSONを、そのまま利用者へ表示した

公開体験版の教材生成で`Unterminated string in JSON`が画面に表示された。OpenAIの
Structured Outputsを使っていても、`max_output_tokens`到達などでresponseが`incomplete`に
なる場合がある。実装は`status`と`incomplete_details`を見ず、途中までの`output_text`を
直接`JSON.parse`していた。さらにrouteが例外文をそのままJSON errorへ入れたため、内部の
parserエラーが利用者に露出した。

対処として、構造化応答の`status`、空出力、JSON解析結果を検査し、途中終了・壊れたJSON・
一時的な429/5xx・通信失敗を1回だけ自動再試行するようにした。教材生成の出力枠を
8,000から16,000 tokenへ増やし、再試行時は最大32,000 tokenとした。再試行後も失敗した場合は
parser文ではなく、日本語の再試行案内だけを返す。途中JSONと連続不正JSONの単体テストも追加した。

**JSON Schema指定は、完全なJSON受信を保証しない。** 構造検証の前にresponse完了状態と
JSONの完全性を検査し、低水準例外を利用者向けAPIへ渡さない。

## 2026-08-12 Skillのデザイン契約を接続せず、APPの教材を完成扱いした

公開APPではブランドロゴと入力UIを整えたが、教材生成AIへ渡していたのは1スライド1テーマ等の
短い一般指示だけだった。`kyozai-slide` Skillが定める白・黒・青、内容別レイアウト、表紙とCTA、
300文字/分、画像検証等はAPPのSchemaとレンダラーへ接続していなかった。さらにHTML教材を
濃紺全面の単一テンプレートで出しながら、公開時に「完成機能」と報告した。

原因は、APPのブランドUIと、利用者が受け取る教材そのもののデザインを同じ課題として扱わなかった
ことにある。Skill文書を読んでいても、機械契約へ移植しなければAPIはその規則を知らない。

対処として`kyozai-standard@1.0.0`の共通profileを作り、Skill・shared・APPの3コピーをテストで
同一検査するようにした。教材Schemaへprofile ID、layout family、比較labelsを追加し、APPの
プレビューとHTML出力を白・黒・青の内容別レンダラーへ変更した。

**ブランドの画面を作ることと、ブランドどおりの成果物を作ることは別である。** 完成扱いの前に、
正本Skillの必須規則がprompt、Schema、renderer、validator、exportへ到達しているか確認する。

## 2026-08-12 共通デザイン版の本番生成が、再試行込みで240秒を超えた

共通profileと拡張Schemaを本番配備した後、実APIで生成・修正を連続確認したところ、生成APIが
クライアントの240秒timeoutへ到達した。生成は1回最大110秒を2回試行する設定で、Vercel処理と
通信の余白を含めるとrouteの240秒上限とほぼ同じだった。ローカルで1回成功したことを、
本番の最悪時間の証明として扱っていた。

生成の1試行を78秒、最大2回の156秒へ制限し、route上限との間に十分な余白を設けた。
**外部APIのtimeout合計は、route上限と同じにしない。** 再試行、JSON処理、通信に必要な余白を
先に確保し、本番URLで最悪経路を確認する。

## 2026-08-12 教材生成が78秒の応答上限へ2回連続で到達した

公開体験版で「AIの応答が途中で終了しました」と表示された。本番ログではOpenAIへの生成が
`TimeoutError`となり、78秒の試行を2回とも完了できていなかった。利用者の入力が短すぎる問題では
ないのに、案内文は入力を短くするよう求めており、原因と対処が一致していなかった。

教材一式を非ストリーミングで待ち、指定研修時間を講師ノートの長さだけで満たそうとしていたため、
構造化JSONが必要以上に長くなった。対処としてResponses APIをSSEで受信し、完成イベントまで
JSONを組み立てるようにした。講師ノートは各スライド120〜240文字の進行要点とし、問いかけ・
演習時間はscenarioへ配分する。生成は1試行105秒、最大2回とし、出力上限は初回10,000、
再試行16,000 tokenへ調整した。エラー案内から入力短縮の要求を削除し、試行名と経過時間を
秘密を含まない構造化ログへ残すようにした。

**同期生成の信頼性はtoken上限だけでなく、応答時間と受信方式で設計する。** 利用者へ入力変更を
求める前に、外部APIのtimeout、出力量、ストリーム完了状態を本番ログで確認する。

独立レビューで、105秒を2回許可するだけではURLの転送取得を含めた最悪時間が240秒を越えると
判明した。route開始時に225秒の共通締切を作り、URLのDNS・fetch・OpenAIの各処理へ渡した。
残り5秒を切る通信は開始せず、各AI試行のtimeoutも残り時間内へ縮める。JSON Schemaには表せない
表紙・行動スライド位置等のruntime検証失敗も同じ試行ループで再生成するようにした。

## 2026-08-12 production buildとE2Eを同じ`.next`へ同時実行した

時間短縮のためbuildとPlaywright E2Eを並列実行したところ、buildが`.next`を再作成中にE2Eの
production serverが起動し、`Could not find a production build`で失敗した。さらに先のbuildが
終了する前に再実行し、`Another next build process is already running`も発生した。

残存build processと`BUILD_ID`を確認し、build、E2Eの順に直列実行して双方成功した。
**同じ`.next`を読む検証と書き換えるbuildは並列化しない。**

## 2026-08-13 「同じ工程」を「生成結果の完全一致」と解釈した

利用者の「SkillとAPPを揃える」という指示を、同じ入力から同じPNG、同じartifact hashを得る要求と
解釈し、共通jobで1回だけ生成する計画を作った。利用者が求めていたのは、AI生成結果の完全一致ではなく、
入力取得、教材設計、内容凍結、画像生成、画像QA、修正、納品を同じ水準で行うことだった。

AI生成物は独立実行ごとに変わる。完全一致を前提にすると、現行Skillを変更しないという条件と衝突し、
APPへ必要な工程を実装する本来の課題から外れる。前回監査で「同一job、同一PNG、Skill/APP間hash一致」を
必須とした判断は撤回する。

今後「同じ」「揃える」という指示を設計へ落とす際は、結果、工程、品質基準、UI、データ形式のどの軸を
指すかを区別する。KYOZAIでは工程同等性をstage ledgerと工程契約テストで証明し、生成結果のpixel比較を
合格条件にしない。APP内部のプレビューと納品物の一致は、別の内部整合性として引き続き検査する。

## 2026-08-13 Skill初期化のUI説明文字数を満たさなかった

`kyozai-revise`を`init_skill.py`で初期化した際、`short_description`を21文字で渡し、規定の
25〜64文字を満たさずメタデータ生成が失敗した。Skill本体のscaffoldは作成済みで、秘密情報の露出や
既存ファイルの破損はない。説明文を規定内へ直し、`generate_openai_yaml.py`で不足したメタデータだけを
生成する。今後は初期化前にUI文字列の文字数を確認する。

同じ作業で`quick_validate.py`をWindows既定localeのまま実行し、UTF-8の日本語`SKILL.md`をCP932で
読もうとして`UnicodeDecodeError`になった。ファイル自体はUTF-8であり、`python -X utf8`で再検証する。
日本語Skillの公式validatorはWindowsではUTF-8モードを明示する。

50件fixture用validatorの初回実行では、計画書の分類名からカテゴリIDを推測して固定し、fixtureで
確定した`add-remove-move`と`source-correction-and-deck-design`に一致せず失敗した。fixtureを変更せず、
validatorを実データのIDへ合わせた。Schemaやfixtureを検査するコードでは、表示名から内部IDを推測しない。

Revision Schemaの確認時、Windowsの`rg`へ`shared/schemas/revision-*.schema.json`を直接渡し、globが
展開されずパス構文エラーになった。ファイル変更や検証結果への影響はない。Windowsでは検索directoryと
`-g 'revision-*.schema.json'`を分けて指定する。

Schemaの実コンパイル検証にPythonの`jsonschema`を使えるか確認したが、環境へ未導入で
`ModuleNotFoundError`になった。構文確認だけで完了扱いにせず、計画書で採用候補としていたAjv 8を
共通開発依存へ追加し、Draft 2020-12としてコンパイル検証する。

Ajv 8の初回厳格コンパイルで、Revision Schemaの条件分岐内にある`contains`と`properties`へ
対象型が明記されておらず、`strictTypes`違反を検出した。validatorを緩めず、条件内にも`array`と
`object`を明記してSchema側を修正した。JSONとして読めることだけをSchema合格条件にしない。

続く厳格コンパイルで、`source.correct`時に必須化する`sourceReferences`が条件ブロック内で
未定義として`strictRequired`違反になった。親Schemaに定義があっても条件側の検査範囲で曖昧にせず、
同じ型と参照を条件ブロックへ明示した。

Revision Planの`version.restore`条件にも、`scope`のobject型と`operations`のarray型、itemsのobject型が
条件ブロック内で不足していたため`strictTypes`違反になった。条件で使うkeywordの対象型を同じ場所へ
明示した。

`oneOf`分岐へのobject型一括補正の初回コマンドでは、PowerShellの単一引用文字列内で引用符を
二重化し、正規表現が一致せず0件で停止した。書込み前に停止したためファイル変更はない。PowerShellの
文字列規則に合わせてpatternを修正した。

Revision Validationにも、条件内の`failureCodes`とscope外配列へarray型がなく、Ajvの厳格コンパイルで
検出された。条件側へ型を追加し、string/nullのunionとpatternの組合せは`anyOf`へ分離した。

差分validatorレポートの`valid: true`条件でも、`summary`と`violations`の型が条件ブロック内に
明示されておらず厳格コンパイルが停止した。objectとarrayを明示し、違反0件という条件を維持した。

Revise統合後、6検証を1つの並列tool callへまとめたところ、外側の64秒制限へ到達し、個別結果を
回収できなかった。検証の合否は不明なので完了扱いにせず、validator群とtypecheck、lint、testを
個別に再実行する。長い検証束は1 callの共通timeoutへまとめない。

production build後のsmoke初回は既定ポート3123が既存プロセスに使われており、安全チェックで停止した。
既存プロセスは停止せず、`SMOKE_PORT=3124`で再実行する。

`SMOKE_PORT=3124`でGit Bashから再実行したsmokeは無出力のまま120秒を超え、外側timeoutで停止した。
停止後に3124のlistenは残っていない。ReviseではAPPコードを変更しておらず、typecheck、lint、既存test、
production buildは成功しているため、smokeはWindows/Git Bash起動経路の未解消事項として分離する。

## 2026-08-13 Phase 0確定前の依存監査とWindows smokeが失敗した

`pnpm audit --audit-level moderate`で、直接開発依存の`ajv 8.17.1`に`$data`利用時のReDoSが
報告された。Schema検証を削除・緩和せず、修正版`8.18.0`へ更新して監査を再実行し、既知脆弱性0件を
確認した。

Windowsで`bash scripts/smoke.sh`を実行するとSystem32のWSL launcherが選ばれ、pnpm引数が壊れて
Next.jsの子プロセスだけが残った。残存processがこのリポジトリの`next start --port 3123`であることを
確認して停止した。Git Bashを明示した再実行では`setsid`未導入で起動できなかったため、Linuxでは従来の
process group、Git BashではWindows process treeを終了する分岐を追加した。受け入れ判定は変更せず、
修正後のHTTP smokeは全項目成功した。

Phase 0 PRのCodeQLは、URL本文からscriptを除く正規表現が空白入り終了タグ`</script >`を
取り逃すとして失敗した。属性付き開始タグと空白入り終了タグを扱う抽出関数へ修正し、script/styleの
内容が教材本文へ入らない回帰テストを追加した。正規表現の警告を無視せず、入力例をテストで固定する。

上記の正規表現修正後も、CodeQLは属性のような文字を含む不正終了タグを取り逃すとして再度失敗した。
HTML構造を正規表現で扱う方針が誤りだったため、`htmlparser2`でDOMを構築し、script/style要素を除去して
text contentを取得する方式へ置き換えた。不正終了タグを含む入力でもscript本文を返さないテストを追加した。

## 2026-08-13 画像生成モデルを利用者確認なしで固定しようとした

工程同等化の実装開始時、調査上の候補だった`gemini-3.1-flash-lite-image`をAPPの固定モデルとして
扱おうとした。利用者の決定は「正本Skillの工程を揃える」であり、画像モデル固定ではなかった。
画像モデルは生成ごとの明示選択へ変更し、未選択をUIとAPIで拒否し、選択値を納品証跡へ残す。

## 2026-08-13 Windows Git Bashでpnpmシェルラッパーを先に選びE2Eが起動しなかった

`scripts/run-pnpm.sh`がGit Bashでも`command -v pnpm`を最初に採用し、シェルラッパー経由で
`--filter`がコマンド名として解釈された。Windows系では`corepack.cmd`を先に選び、引数をそのまま
渡す順へ変更した。E2Eの受け入れ条件は変更していない。

## 2026-08-14 旧Vercelデプロイの再実行を最新mainの反映と誤認した

VercelプロジェクトがGitHubと接続されていない状態で`vercel redeploy`を実行し、最新`main`ではなく
旧デプロイのソースを再ビルドした。環境変数とHTTP 200だけを確認して完了と報告したため、本番には
画像モデル選択UIが存在しなかった。今後は本番HTMLで新機能の固有マーカーを確認し、未接続環境では
作業ツリーから`vercel --prod`でデプロイする。

最新ソースの初回`vercel --prod`では、Vercel時だけ`turbopack.root`を`apps/web`へ狭める設定により、
pnpmワークスペースルートの`next`を解決できずビルドが失敗した。ローカルとVercelでルートを分岐せず、
常にモノレポルートを指定するよう修正した。旧本番は失敗中も維持された。

## 2026-08-14 段階生成への変更後も一括生成前提のテストを残した

教材生成を分析、スライドマップ、講師台本、内容凍結QAへ分けた直後、既存のAI応答テストが
1回のAPI応答で完成教材を返す前提のままで4件失敗した。また工程コードを通信処理と同じファイルへ
追加し、300行上限lintにも失敗した。検査は緩めず、各段階の応答を検証するテストへ置き換え、
教材工程を`content-generation.ts`へ分離した。最終的にtypecheck、lint、42テストを通した。

## 2026-08-23 退避したNext.js生成物をlint対象に残した

過去にWindows/OneDrive上の`.next`削除がEPERMで失敗した際、`.next.stale-build-lock`へ退避したが、
その未追跡生成物が作業フォルダに残り、`pnpm --filter web lint`がTurbopack生成JSを検査して失敗した。
生成物は品質検査対象ではないため、eslintのignoreを`.next*/**`へ広げ、通常の`.next`と退避名の両方を
lint対象外にした。テストやlintルール自体は緩和していない。

## 2026-08-23 本番画像生成でsharp/libvipsのバンドル不足を見落とした

本番`/api/render-slide`は画像生成APIへ到達する前後で`sharp`を読み込むが、VercelのLinux実行環境で
`@img/sharp-libvips-linux-x64`が関数バンドルに入らず、`libvips-cpp.so`を読めずにクラッシュした。
クライアント側は非JSONのサーバーエラーを`response.json()`で読もうとしていたため、利用者には
「画像生成の応答が途中で終了しました」とだけ表示され、根本原因が隠れた。

本番ログを確認せずに「画像生成工程まで到達する」と判断したのが誤りだった。`sharp`をVercelで
安定して動く版へ固定し、Linux native依存を`outputFileTracingIncludes`へ明示し、install時に
optional依存を有効にする。さらに完成PNGをbase64 JSONで返す設計はVercelの4.5MB応答上限に当たるため、
PNGをパレット最適化し、上限超過を実画像テストで検出する。

## 2026-08-24 公開生成経路の境界防御と運用文書が不足していた

公開APPのクロスレビューで、画像生成grantの署名鍵がOpenAI API鍵へfallbackする用途混在、
プロセス内Mapだけのレート制限、DNS検査後にfetchが再解決するSSRFのTOCTOU、chunked応答を
全量読み込んだ後に切り詰める無制限ボディ読み込みが見つかった。最も費用と情報流出の影響が
大きい公開境界に、private IP、DNS rebinding、実受信量、複数インスタンスの回帰テストが無かった。

また、`.env.example`の停止値を本番状態と取り違えられる構成だったが、実際のProductionには
`PROCESS_PARITY_PIPELINE_ENABLED=1`が設定済みだった。リポジトリの既定値だけを本番稼働状態の
証拠にしてはならない。本番は環境変数で上書きできないコード境界で生成UIとAPIを封鎖し、
healthのcommit SHAとProductionへのHTTP検査を外部事実として確認する。

READMEは旧リポジトリ名と旧Skill 1件だけを案内し、KYOZAI-v1の6つの正本Skill、公開APP、
限定公開状態から乖離していた。独自auto-mergeも人間承認を要求せず、Production smokeはpresetに
留まって実行されていなかった。READMEを現行構成へ更新し、auto-mergeを廃止して、CI、最新差分への
承認1名、会話解決、管理者への規則適用をbranch protectionで必須化する。

## 2026-08-24 手動本番配備へ誤ったcommit SHAを指定した

Git連携を使わないVercel配備の証拠を`/api/health`へ載せる際、配備直前に`git rev-parse HEAD`を
取得せず、誤った文字列を`KYOZAI_DEPLOY_COMMIT_SHA`へ指定した。生成APIの404封鎖には影響せず、
healthの配備元証拠だけが実commitと不一致になった。配備コマンド内で`git rev-parse HEAD`を取得し、
その出力を直接環境変数へ渡して再配備する。SHAを目視で転記・補完しない。

## 2026-08-24 公開AI生成を成立条件なしで先に公開した

### 本来どう設計すべきだったか

公開画面、生成API、外部AI呼び出しを同じ「公開済み」として扱ったことが誤りだった。画像生成は
1要求が高額・長時間・外部依存であり、通常の同期フォーム送信として公開してはならない。最初の
公開順序は次のとおりにすべきだった。

1. 本番は成果紹介だけを公開し、生成はVercel認証済みPreviewまたはローカルに限定する。
2. 専用署名鍵、分散かつfail-closedな利用上限、全体費用上限、WAF、入力URLの接続先固定、実バイト上限を、生成APIより先に実装・実環境検証する。
3. 画像・ZIPをリクエスト中に全量返さず、永続job、private artifact storage、進捗照会へ移す。同期routeのtimeoutやJSON body上限を製品の前提にしない。
4. その状態で攻撃境界・課金上限・provider障害・実画像生成を含むPreview E2EとProduction smokeを通し、費用停止flagと運用手順を確認してから、公開生成の可否を判断する。

### 最初に気付かなかった原因

Skill/APPの工程同等化、画像品質、Vercel上の`sharp`起動を優先し、「匿名の利用者が任意回数の
高額な外部APIを叩ける公開境界」を独立したリリースゲートとして扱わなかった。ローカルの単一
プロセス試験はMapの非永続性、DNS再解決、chunked本文、serverlessの複数instance、Productionの
秘密設定不足を露出しない。さらに、生成成功の確認を優先して、失敗時の費用・入力到達範囲・
タイムアウト・配備証拠を最初の設計レビュー項目に入れなかった。

以後、外部課金APIまたは外部URL取得を公開する変更は、実装着手前に「誰が呼べるか、1回の最大費用、
全体停止条件、入力が到達する先、responseをいつどこへ保存するか、provider停止時の挙動」を設計書と
テスト計画へ固定する。この項目が未決定なら、公開生成を有効にしない。
## 2026-08-25 永続jobのschemaを完成実装と取り違えた

永続job用のテーブル、RLS、outbox、Workflow SDKを追加した段階で、実際に受理jobが起動・中断回復・費用制限・成果物検証まで到達するかを破壊試験で確認しなかった。Production Cronは内部routeを404にして起動不能、Previewには定期dispatcherがなくpendingのまま停止した。また所有者向け`for all` RLSが受付後の入力・画像モデル・状態を本人に書き換え可能にしていた。schemaを置くことは境界を実装した証拠ではない。

今後は、公開前に少なくとも「dispatcher開始直後の喪失」「Workflow step停止」「期限切れlease」「同時配送」「provider呼出直前の予算確認」「Storage読戻しhash不一致」「所有者の直接更新」を意図的に壊して検証する。これらのいずれかがfail-closedでない限り、Productionの生成経路は404のまま維持する。
## 2026-08-26 G0の合格条件へG6のSkill／APP比較条件を混入した

- **何が起きたか**：正本計画ではG0の実証拠は正本Skillの実packageであるのに、handoffで
  同一入力のSkill／APP両packageを要求し、G1のPreview環境不足をG0の停止理由にした。
- **影響**：G0単独で実行可能な正本Skill検証を進めず、次Gateの外部条件を待つ状態にした。
- **原因**：最終ゴールのペア比較条件と、現Gate固有の合格条件を分離せずに要約した。
- **修正**：実行計画、goal contract、handoffのG0証拠を
  `canonical_skill_real_package_accepted`へ統一した。Skill／APPの全fixture比較はG6に維持する。
- **再発防止**：停止理由を記録する前に、実行正本の現Gate行とgoal contractの
  `acceptanceEvidence`を照合する。

## 2026-08-26 モンタージュ作成前に利用可能な画像処理経路を確認しなかった

- **何が起きたか**：正本Skill実packageの画像寸法確認で、未導入のImageMagickを実行して失敗した。
  続けてworkspace dependency取得機能もこの環境ではhandler未登録で失敗した。またhandoff更新時に、
  同一パッチ内で同じファイルを削除・再追加しようとして編集機構に拒否された。
- **影響**：画像生成物やリポジトリの既存内容への変更はなく、検証開始と記録更新が遅れた。
- **修正**：リポジトリに導入済みの`sharp`で寸法確認とモンタージュ生成を完了し、handoffは内容置換で更新した。
- **再発防止**：画像処理はアプリが実際に利用している`sharp`を第一候補とし、既存ファイルの全置換も
  `Update File`で行う。

## 2026-08-26 G0のstage後差分検査で末尾空白を検出した

- **何が起きたか**：新しい実行正本の制定日と状態の2行にMarkdown改行用の末尾空白があり、
  `git diff --cached --check`が不合格になった。
- **影響**：コミット前に検出したため、CI、公開物、履歴への影響はない。
- **修正**：該当2行の末尾空白を除去した。
- **再発防止**：Markdownでも意図的な末尾空白を使わず、段落または空行で改行を表現する。

## 2026-08-26 G1監査でWindows非対応のglobと存在しないテスト名を指定した

- **何が起きたか**：migration検索へ`supabase/migrations/*.sql`を直接渡し、PowerShell上で期待どおり
  展開されず読取に失敗した。また、存在確認前に`apps/web/tests/content-generation.test.ts`を指定した。
- **影響**：読取専用コマンドだけが失敗し、ファイル変更や検査結果への影響はない。
- **修正**：`rg`へdirectoryを渡し、`rg --files`と実在するテスト名から監査を再開した。
- **再発防止**：Windowsでは検索対象directoryを直接渡し、個別ファイルは`rg --files`で存在確認してから読む。

## 2026-08-26 G1本文provider回収の大きなpatchがcontext不一致で失敗した

- **何が起きたか**：`openai.ts`の広い範囲を一度に置換しようとして、既存コードとpatch contextが一致せず
  適用されなかった。
- **影響**：編集機構が全体を拒否したため部分適用はなく、作業時間だけを失った。
- **修正**：対象関数を小さい範囲で読み直し、`requestStructured`単位のpatchとして適用した。
- **再発防止**：通信・再試行処理の変更は関数境界で最新内容を再取得してから編集する。

## 2026-08-26 G1テストfixtureが同じResponseを再利用して回収不能と誤判定された

- **何が起きたか**：壊れたJSONを2回返すテストが、bodyを一度しか読めない同一`Response` instanceを
  返していた。2回目はJSON内容ではなく消費済みbodyで失敗し、曖昧なprovider結果として停止した。
- **影響**：全件テスト148件中1件が一時不合格になった。実provider経路や成果物は変更していない。
- **修正**：各fetch呼出しへ新しい`Response`を生成するfixtureへ直し、期待する壊れたJSON検査を復元した。
- **再発防止**：複数回呼ばれるfetch mockは`mockImplementation`で呼出しごとに新しいbodyを返す。

## 2026-08-26 G1回収処理の追加で画像rendererの行数上限を超えた

- **何が起きたか**：画像生成と画像QAのcheckpoint処理を`image-renderer.ts`へ直接追加し、lintの
  300行上限を339行で超えた。
- **影響**：lintが1件不合格になり、コミット前に停止した。
- **修正**：provider結果の保存・回収を`image-provider-recovery.ts`へ責務単位で分離した。
- **再発防止**：provider状態機械は描画実装から独立させ、rendererはprovider呼出しと画像検証の組立てに限定する。

## 2026-08-26 G1 timeout再送廃止後に到達不能な型分岐を残した

- **何が起きたか**：結果不明timeoutをその場でterminalにする変更後も、関数末尾に旧`timeout`分岐を残し、
  TypeScriptが到達不能な比較として検出した。
- **影響**：全151テストとlintは合格したが、typecheckが1件不合格になった。
- **修正**：到達不能な状態と末尾分岐を削除し、timeoutは発生箇所で曖昧計上後に明示終了する形へ統一した。
- **再発防止**：再試行状態を減らす変更では、状態union、代入、末尾のerror mappingを同時に更新する。

## 2026-08-26 G1 Preview確認前にSupabase CLIとDockerの利用可否を確認しなかった

- **何が起きたか**：最初のSupabase CLI確認は未導入で失敗し、一時実行へ切り替えた後にCLI認証が
  無いことが分かった。local migration検証用Docker daemonも起動していなかった。
- **影響**：外部状態は変更しておらず、秘密値も表示していない。Preview migration適用とlocal DB検証は未実施。
- **修正**：一時CLIではproject一覧の読取だけを試し、認証要求で停止した。Vercelは環境変数名だけを確認し、
  Previewには`GEMINI_API_KEY`以外の必須設定が無いことを外部ブロッカーとしてhandoffへ記録した。
- **再発防止**：Gate開始時の環境監査に、CLI存在、CLI認証、Docker daemon、Preview環境変数名の確認を含める。

## 2026-08-26 G1 Previewをrepo rootから配備してWeb設定を適用できなかった

- **何が起きたか**：手動Preview配備をrepo rootから実行し、`apps/web/vercel.json`ではなくproject側の
  `public`出力設定でbuild後検査が失敗した。
- **影響**：失敗したPreview deploymentが1件作られた。Productionと既存deploymentは変更されていない。
- **修正**：`apps/web`を配備単位として明示して再実行し、実設定の検証へ進んだ。
- **再発防止**：このmonorepoの手動Vercel配備は常に`--cwd apps/web`を指定し、root配備を使わない。

## 2026-08-26 G1 dispatcher CronがVercel Hobbyの頻度制限で配備不能だった

- **何が起きたか**：実配備設定へ統合した5分間隔dispatcher CronをVercelが拒否した。現在のaccountでは
  Cronは1日1回までで、`*/5 * * * *`を含むPreviewを作れない。
- **影響**：失敗したPreview deploymentが1件追加され、G1の実DB／Provider縦断へ進めない。
  Productionと既存deploymentは変更されていない。
- **修正状況**：`G1-CRON-002`として停止し、現行Cronを利用できるplanへ変更するか、Hobby上で即時dispatchと
  有限時間retryを保証する構成へ実行計画を変更するか、利用者判断を待つ。
- **再発防止**：配備設定をGate実装する前に、対象accountのplan制限を実deployで確認する。

## 2026-08-26 監査補正の新schemaでAjv設定不整合を検出した

- **何が起きたか**：新しいgoal schemaの`pattern`に型指定がなく、strict Ajvがcompileを拒否した。
  また日時format plugin未導入のままstrict format検証を要求し、blind evidence validatorのテストが失敗した。
- **影響**：ローカルの新規検証器だけが不合格になった。既存package、CI、外部証拠、秘密値への影響はない。
- **修正**：`id`をstringとして明示し、日時はschemaの未登録formatに依存せず、validatorで実際にparse・順序比較する
  方式へ統一した。
- **再発防止**：Ajv schema追加時はstrict compileと正・負の実データ検証を同じ変更で実行する。

## 2026-08-27 0円scheduler実装の調査で存在しない対象を読んだ

- **何が起きたか**：最初の検索で存在しない`tests` directoryを指定し、続く読取で誤ったmigration名と
  PowerShellの角括弧pathをそのまま渡したため、対象を取得できなかった。
- **影響**：読取専用コマンドだけが失敗した。ファイル変更、外部設定、秘密値、実行状態への影響はない。
- **修正**：実在する`apps/web/tests`とmigration一覧を基準に読み直し、Next.jsのdynamic routeはliteral pathとして扱う。
- **再発防止**：検索・読取の前に`rg --files`で対象名を確認し、PowerShellではdynamic route pathを`-LiteralPath`で読む。

## 2026-08-27 scheduler変更の検証でNext buildを並列起動した

- **何が起きたか**：先行`next build`の完了前に、buildを含む別の検証を並列に開始した。後続buildは`.next/lock`を
  検出して停止した。
- **影響**：後続のローカルbuildだけが不合格になった。実装、外部配備、秘密値、CIへの影響はない。
- **修正**：起動中のbuildが無いことをprocess command lineで確認し、検証用serverを停止してstale lockを除去した後、
  build、smoke、E2Eを直列で再実行する。
- **再発防止**：Next.jsのbuild、smoke、E2Eは同じ`.next`を使うため、これらを並列に起動しない。

## 2026-08-27 build lockの停止方法に関する訂正

- **訂正対象**：直前の「scheduler変更の検証でNext buildを並列起動した」の修正記述。
- **誤り**：検証用serverを停止してlockを除去したと記録したが、停止・削除コマンドは実行環境の安全制約で拒否された。
- **正しくは**：その後に起動中processが無く、lockも存在しないことを読取で確認した。理由はプロセスが自然終了したためであり、
  エージェントが停止・削除したためではない。
- **影響**：記録の訂正のみ。実装、外部環境、秘密値への影響はない。

## 2026-08-27 Browser Skillの旧cache pathを最初に参照した

- **何が起きたか**：Browser Skillの案内にあるcache pathのversionが現在のinstalled versionと一致せず、最初の読取が失敗した。
- **影響**：読取専用の失敗だけで、ブラウザ操作、外部設定、秘密値への影響はない。
- **修正**：installed plugin cacheから現在のversionの`SKILL.md`を特定して読み直した。
- **再発防止**：plugin cacheを参照する前に、実在するversion directoryを確認する。

## 2026-08-27 外部の鍵生成ページ調査で一時候補値がツール出力へ現れた

- **何が起きたか**：鍵生成ページの安全性を調査した際、ページが自動表示した一時候補値が調査用ツール出力に含まれた。
- **影響**：その値は運用へ登録・送信・保存しておらず、認証や利用者データへの影響はない。値は引用せず破棄する。
- **修正**：当該外部ページを運用鍵の生成方法として採用しない。
- **再発防止**：運用鍵の候補値を自動表示する外部ページはエージェントが開かない。利用者自身がローカルで生成し、エージェントを経由せず直接登録する。

## 2026-08-27 外部の鍵生成ページ調査で候補値をツール出力へ露出した

- **何が起きたか**：外部の鍵生成ページ本文を調査ツールで取得した際、ページがその場で返した候補値がツール出力に含まれた。
- **影響**：候補値はコピー・保存・環境変数への登録をしておらず、運用の秘密情報ではない。露出経路は調査ツール出力のみである。
- **修正**：候補値を再表示・引用せず、外部生成サイトを使わないローカル生成手段へ調査方針を切り替えた。
- **再発防止**：秘密値を扱う調査では、値を含むページ本文を取得せず、公式ドキュメントや仕様ページだけを根拠にする。

## 2026-08-27 Vercelの環境選択を画面確認前に案内した

- **何が起きたか**：Git未接続projectのbranch限定環境変数を、実際のVercel UIで保存できることを確認する前に案内した。
  UIは保存を拒否し、既存の`CRON_SECRET`がProductionだけへ保存されていることも後から確認した。
- **影響**：Productionでprovider呼出しは行われず、秘密値の表示・共有もない。誤ったProduction設定は利用者が削除し、
  新しいSecretをPreviewだけへ登録した。
- **修正**：新規追加ではなく既存Secretの編集画面を実際に確認し、環境一覧の`Preview`だけを選ぶ手順へ訂正した。
- **再発防止**：外部UIの設定手順は、公式仕様に加えて同じaccountの入力画面で保存前の選択肢・警告・保存可否を確認してから、
  1ステップずつ案内する。

## 2026-08-27 Vercelのmonorepo配備境界を誤ってroot配備へ切り替えた

- **何が起きたか**：`apps/web`だけのCLI配備が`shared/`契約を含めず失敗したため、root packageをNext.js projectとして
  配備する設定へ一時的に切り替えた。root packageにはNext.js dependencyが無く、Vercel buildはframework検出で停止した。
- **影響**：Preview deploymentが2件不合格になった。Production、provider呼出し、秘密値の表示・共有には影響がない。
- **修正**：root配備用の設定を戻した。Vercel公式のmonorepo方式どおり、`apps/web`をRoot Directoryに保ち、
  親`shared/`をBuild対象へ含めるproject設定を確認してから再配備する。
- **再発防止**：monorepoの外部importは、framework検出だけでrootへ移さず、Root Directoryとsource-inclusionの
  platform設定を公式仕様で先に確認する。

## 2026-08-28 Windows/Node 24でWrangler dry-runがbundle後に異常終了した

- **何が起きたか**：`apps/control-plane`の`wrangler deploy --dry-run`は、TypeScript bundleとD1/R2 bindingの
  解決を完了して`--dry-run: exiting now.`を表示した後、Windows上でexit status `0xC0000409`となった。
  Wranglerを4.110.0から4.100.0へ下げても再現した。
- **影響**：Cloudflareへの実配備、D1/R2の実操作、Vercel設定、秘密値の表示・共有は行われていない。
  local D1 migration、control-planeの型検査、境界テストは別途合格している。
- **修正状況**：dry-runを成功として扱わず、Windows/Node 24とWranglerの互換性として切り分けを継続する。
  deploy前にはCIまたはCloudflare実行環境で同じdry-runを再実行する。
- **再発防止**：Workerのローカル検証は型検査、契約テスト、local D1 migration、dry-runを独立して記録し、
  bundle後のプロセス異常をbuild成功と読み替えない。

## 2026-08-28 local Worker stateを初回commitへ含めた

- **何が起きたか**：control-planeのlocal D1/R2検証で生成された`apps/control-plane/.wrangler/`を
  ignoreする前に初回commitへ含めた。
- **影響**：含まれたのは空のlocal schema/cacheとbundle一時出力だけで、案件データ、運用設定、秘密値はない。
  当該commitはpushしていない。
- **修正**：stateを削除して`.gitignore`へ追加し、push前に未push commit履歴をまとめ直してstateを履歴から除外する。
- **再発防止**：local Workerを起動する前に`.wrangler/`をignoreし、commit前に`git status --short`で
  runtime stateを確認する。

## 2026-08-28 WindowsでWrangler更新後もdry-runが異常終了した

- **何が起きたか**：CI監査で検出された`ws`の高重大度脆弱性を解消するため、Wranglerを4.126.0と
  互換の`@cloudflare/workers-types` 5.20260825.1へ更新した。型検査、Worker境界テスト、依存監査は
  合格したが、Windows/Node 24の`wrangler deploy --dry-run`は再びbundle完了後に`0xC0000409`で終了した。
- **影響**：実配備、Cloudflareリソース操作、Vercel設定、秘密値の表示・共有はない。CIのLinux dry-runは
  旧版で合格しており、更新版について改めてCIで確認する。
- **修正状況**：Windowsの異常終了を合格として扱わず、Linux CIでbuildと監査を実証する。供給網ポリシーは
  緩めず、公開後24時間を経過した依存版を選定した。
- **再発防止**：WindowsでWorker bundleが表示上完了しても、終了コードが非0なら検証不合格として扱い、
  CIの対象OSで再現性を確認する。

## 2026-08-28 local D1 fixtureのJSON SQL引数がPowerShellで壊れた

- **何が起きたか**：`wrangler d1 execute --command`へ渡したJSON配列を含むSQLがPowerShellの引数解釈で分割され、
  Wranglerはunknown argumentとして停止した。
- **影響**：直前のlocal D1 migrationは24命令すべて成功したが、system controlの更新は実行されていない。
  remote D1/R2、Vercel、秘密値、案件データへの影響はない。
- **修正状況**：値をSQL引数へ直接埋め込まず、JSON関数で表現できる引数へ変更してfixtureを再実行する。
- **再発防止**：PowerShell経由のWrangler SQLでは、入れ子の引用符を含むliteralを避け、DB側のJSON関数または
  SQL fileを使用する。

## 2026-08-28 internal Bearer認証が空白を一致していなかった

- **何が起きたか**：local Worker fixtureで正しいinternal tokenを送っても404になった。認証正規表現が
  正規表現の空白クラスではなく文字列`\\s`を探しており、`Bearer <token>`を一致できなかった。
- **影響**：internal commandはすべてfail-closedで404となり、D1の書込みは実行されなかった。remote環境、
  利用者データ、秘密値への影響はない。
- **修正**：空白クラスを正しく解釈する正規表現へ変更し、正しいBearer headerが400（入力形式エラー）まで
  到達する境界テストを追加する。
- **再発防止**：拒否だけでなく、認証済みrequestが次の入力検証段へ到達するテストを必須にする。

## 2026-08-28 stage完了を拒否するD1制約があった

- **何が起きたか**：local D1 fixtureでstageを`passed`へ更新したところ、初期schemaの
  `status = 'running'`と`started_at IS NOT NULL`の同値制約が、完了後も保持すべき`started_at`を禁止して停止した。
- **影響**：Workerは503でfail-closedとなり、stage・jobの状態は更新されなかった。remote D1/R2、Vercel、
  provider、秘密値への影響はない。
- **修正**：制約を「runningのstageにはstarted_atが必須」へ変更し、既存D1 tableを安全に再構築するmigrationを追加する。
- **再発防止**：claim後のpass/fail fixtureでstarted_at、completed_at、terminal statusの組を検証する。

## 2026-08-28 provider accounting追加後もWindowsのWrangler dry-runが異常終了した

- **何が起きたか**：G1のprovider accounting migrationとcancellation settlement commandの追加後、
  `pnpm -r build`を再実行した。Web buildとWorker bundle、D1/R2 binding解決、`--dry-run: exiting now.`の表示後に、
  Windows/Node 24のWorker processだけがexit status `0xC0000409`で終了した。
- **影響**：Cloudflareへの配備、remote D1/R2、Vercel設定、provider呼出し、秘密値の表示・共有は行われていない。
  control-plane型検査・境界テスト、local D1 migration、local cancellation fixtureは別途合格している。
- **修正状況**：このローカルdry-runを成功と扱わず、push後のLinux CI buildを合格証拠として確認する。
- **再発防止**：Windowsで同じ終了コードが再発した場合も、bundle表示だけで合格にせず、実装検証とCI検証を分けて記録する。

## 2026-08-28 private R2 clientでNode BufferをFetch bodyへ直接渡した

- **何が起きたか**：Vercel Workflowからcontrol-planeへartifact bytesをstreamするclientの型検査で、
  Nodeの`Buffer`がFetchの`BodyInit`として受理されず停止した。
- **影響**：型検査段階で停止しており、R2/D1への書込み、provider呼出し、秘密値の表示・共有はない。
- **修正**：`Uint8Array`入力をserver-sideの`Blob`へ明示変換してからFetch bodyに渡す。
- **再発防止**：Node runtime固有のbinary型をHTTP境界へ渡す処理は、型検査とreadbackテストで検証する。

## 2026-08-28 private R2 clientのBlob変換が共有buffer型を受理しなかった

- **何が起きたか**：前項の修正で`Uint8Array`をそのまま`Blob`へ渡したところ、型定義上はshared bufferを
  持ち得るviewとして拒否された。
- **影響**：型検査段階で停止しており、R2/D1、provider、秘密値への影響はない。
- **修正**：`Uint8Array.from`で専用のarray bufferへコピーしてから`Blob`を構成する。
- **再発防止**：NodeとDOMのbinary型境界では、buffer所有権を型で明示して検査する。

## 2026-08-28 D1 provider settlementの行型が不十分だった

- **何が起きたか**：provider settlement commandの型検査で、D1の`first()`結果を直接cost値として扱い、
  戻り値のnull／scalar表現を十分に狭めていないことが検出された。
- **影響**：型検査段階で停止しており、D1/R2、provider、秘密値への影響はない。
- **修正**：cost rowを明示的に検証し、値が不正ならquota更新前に競合としてfail-closedにする。
- **再発防止**：D1のdynamic rowは外部境界と同じく、使用前に型を検証する。

## 2026-08-28 D1 provider trigger後の更新件数を成功判定に使った

- **何が起きたか**：local D1 fixtureでprovider settlementはusageとquotaを正しく更新したが、
  triggerを伴う`UPDATE`のreturned change countを一律に1と仮定したため、gatewayが409を返した。
- **影響**：fixture stateだけで起き、provider実呼出し、remote D1/R2、秘密値への影響はない。
- **修正**：更新件数ではなく、同一usage rowのcharge state readbackでsettlement成功を確認する。
- **再発防止**：D1 triggerを伴う更新では、driverの件数だけを結果契約にせず永続stateを再読込して確認する。

## 2026-08-28 D1 provider reservationでもtrigger後の更新件数を成功判定に使った

- **何が起きたか**：同じlocal D1 fixtureで、reservation insertはusage rowとinflight quotaを正しく作成したが、
  triggerを伴うinsertのreturned change countを一律に1と仮定したため初回requestへ409を返した。
- **影響**：fixture stateだけで起き、provider実呼出し、remote D1/R2、秘密値への影響はない。
- **修正**：insertが例外なく完了したことを初回reservation成功とし、unique conflict時だけ既存rowを読んで再送扱いにする。
- **再発防止**：D1 triggerのあるcommandでは、各DMLのreturned change countを成功契約へ使わない。

## 2026-08-28 PowerShell経由のartifact fixture SQLがJSON引用で分割された

- **何が起きたか**：local D1のartifact read fixtureを`--command`で実行した際、metadataとoutput IDのJSON literalが
  PowerShell引数解釈で分割され、WranglerはSQLを実行せずunknown argumentで停止した。
- **影響**：fixture DBへの書込みはなく、remote D1/R2、provider、秘密値への影響はない。
- **修正**：JSONを含むD1 fixtureはSQL fileへ置き、`--file`で実行する。
- **再発防止**：PowerShellからWranglerへJSONを含むSQLを渡す場合はinline commandを使わない。

## 2026-08-28 Workflow helperがファイル行数上限を超えた

- **何が起きたか**：private R2 artifact処理を`job-workflow.ts`へ追加した結果、CIのlintでファイルが314行となり、
  `max-lines`の300行制約により失敗した。
- **影響**：型検査までは合格しており、CIはlint時点で停止した。remote D1/R2、Vercel、provider、秘密値への影響はない。
- **修正**：artifactの永続化・readback・既存画像復元を単独の`job-workflow-artifacts.ts`へ分離した。
- **再発防止**：workflow組立ファイルへstorage実装を積み増さず、単独で変更・検証される責務ごとに分ける。

## 2026-08-28 WindowsのWorker dry-runが再度異常終了した

- **何が起きたか**：workflow state command追加後の`pnpm -r build`でも、Worker bundleとbinding解決、
  `--dry-run: exiting now.`の後にWindows/Node 24のWrangler processが`0xC0000409`で終了した。
- **影響**：Cloudflareへの配備、remote D1/R2、Vercel、provider、秘密値への影響はない。web buildはこの時点まで開始済みだったが、
  recursive buildはcontrol-planeの異常終了により失敗扱いとなった。
- **修正状況**：既知のローカル環境問題として成功扱いにせず、Linux CIのbuild証拠で判定する。実装側のtypecheck、lint、unit test、local D1 fixtureは別途実行する。
- **再発防止**：この環境でWorker buildを実行した場合は、exit codeとCIのLinux結果を必ず分けて記録する。

## 2026-08-29 Cloudflare Cron cleanupのHTTP methodがVercel routeと一致していなかった

- **何が起きたか**：control-plane schedulerはdispatchとcleanupをPOSTで呼ぶが、cleanup routeはGETだけを公開していた。
- **影響**：Cloudflare Cronからcleanupを起動すると405となり得た。Preview/ProductionのCron、D1/R2、利用者データ、秘密値への実影響はない。
- **修正**：cleanup処理を共通化し、Vercel Cron用GETとCloudflare scheduler用POSTの両方を同じ認証境界で受けるようにした。
- **再発防止**：scheduler targetごとにmethodを契約テストで確認し、Cron呼出し側とroute側を同時に変更する。

## 2026-08-29 WindowsのG1 direct-text fixtureがOS既定一時領域へ書き込めなかった

- **何が起きたか**：`mktemp`がWindowsの`C:\\tmp`を選び、Wranglerのlocal D1 migrationがpermission errorで停止した。
- **影響**：migration開始前に停止し、remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：fixtureの一時状態をリポジトリ配下の`outputs/tmp`に明示的に作成し、終了時に削除する。
- **再発防止**：Windowsで実行するlocal Worker fixtureはOS既定の一時領域に依存せず、プロジェクト管理下の隔離ディレクトリを指定する。

## 2026-08-29 WSLからWindows版WranglerへPOSIXのpersist pathを渡した

- **何が起きたか**：前項の修正後も、WSL上の`/mnt/c/...`をWindows版Wranglerへそのまま渡したため、`C:\\mnt\\...`として解釈されpermission errorで停止した。
- **影響**：local D1 migrationの開始前に停止し、remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：WSL環境では`wslpath -w`でpersist pathだけをWindows形式に変換してWranglerへ渡す。
- **再発防止**：WSLからWindows実行ファイルを呼ぶfixtureでは、ファイル引数の形式を呼出し境界ごとに明示する。

## 2026-08-29 fresh D1 migrationでprovider accounting columnが重複した

- **何が起きたか**：`0001_g1_schema.sql`へ後続migration `0004_provider_accounting.sql`の追加columnを先取りして書いていたため、fresh databaseで`0004`がduplicate column errorとなった。
- **影響**：isolated local fixtureのmigration中に停止し、remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：先取りしたcolumnを`0001`から外し、既存環境とfresh環境の両方で`0004`が唯一のschema追加元となるようにした。
- **再発防止**：適用済みmigrationのschemaを後続migrationと重ねて編集せず、fresh migration fixtureをschema変更ごとに実行する。

## 2026-08-29 WSLのfixture health checkがWindows Workerへ到達しなかった

- **何が起きたか**：Windows版Wranglerが起動した`127.0.0.1`へWSL版`curl`で接続したため、Workerはreadyでもhealth checkが失敗した。またcleanupもWindows子processを停止できなかった。
- **影響**：isolated local fixtureが停止し、一時stateが残った。remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：WSL上ではWindows版`curl.exe`でHTTP確認を行い、port listenerをWindows側で停止してstateを削除する。
- **再発防止**：local Workerの実行環境とHTTP clientのnetwork namespaceを一致させ、fixture cleanupはWorker processの所有OSで実行する。

## 2026-08-29 WSLのfixture JSON assertionにLinux側Nodeを仮定した

- **何が起きたか**：WorkerとHTTP clientをWindows側へ合わせた後も、fixtureのJSON assertionはWSLの`node`を呼んでおり、Node未導入のWSLでは初回command後に停止した。
- **影響**：isolated local fixtureの途中で停止し、remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：WSLではWindows版`node.exe`を使い、HTTP responseのpathもWindows形式で渡す。
- **再発防止**：cross-OS fixtureではWorker、HTTP client、assertion runtimeを同じOS側へ明示的に揃える。

## 2026-08-29 local Worker起動中に同じD1 stateへfixture SQLを投入した

- **何が起きたか**：Windows版Workerがlocal D1 stateを使用中に別のWrangler processからfixture用stage/artifact rowを書き込もうとして、SQLiteのlock待ちになった。
- **影響**：isolated local fixtureがsnapshot検証前に停止し、remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：fixture用rowの投入前にWorkerを停止し、D1書込み後に同じstateで再起動する。
- **再発防止**：local D1 fixtureではWorkerを通す検証と直接D1投入を同時に行わず、state所有者を段階ごとに一つにする。

## 2026-08-29 Windows Wrangler子processの終了待ちがfixtureを不安定化した

- **何が起きたか**：WSLから起動したWindows版Wranglerが親shellの終了後もchild processを残し、次回fixtureのport確認とlocal D1再利用を不安定にした。
- **影響**：isolated local fixtureの完走証拠は未取得。remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正状況**：fixtureにlistener停止と停止完了待ちを追加した。Windows固有のchild-tree終了はLinux CIでも検証してから合格証拠にする。
- **再発防止**：OSをまたぐRunnerの終了コードだけでfixtureを合格扱いにせず、port解放とclean state作成を明示確認する。

## 2026-08-29 WSL fixture cleanupがWindows Workerの所有者を特定できなかった

- **何が起きたか**：WSL shellでbackground起動したWindows版Wranglerは、POSIX側のbackground PIDとは別のWindows process treeとして残った。従来のcleanupはそのPIDを`kill`した後、command lineとport番号だけで残存processを探索していたため、fixture自身が開始していないlistenerまで停止対象になり得た。
- **影響**：isolated local fixtureのport解放とstate削除が不安定だった。remote D1/R2、provider、利用者データ、秘密値への影響はない。
- **修正**：WSLではPowerShell `Start-Process`でWindows側の親PIDを取得し、そのPIDだけを`taskkill /T`で終了する。終了後はport解放を待機し、未解放なら任意のlistenerをkillせずfixtureを失敗させる。
- **再発防止**：cross-OS processを起動するfixtureは、開始時に所有者PIDを取得し、cleanupで名前・portだけの探索を使わない。

## 2026-08-29 Windows Wrangler dry-runがビルドを異常終了させた

- **何が起きたか**：`pnpm -r build`で`apps/control-plane`の`wrangler deploy --dry-run`がbinding解決とbundle出力後にWindows終了コード`3221226505`で異常終了した。
- **影響**：ローカルのcontrol-plane build証拠を取得できなかった。生成経路、remote D1/R2、provider、秘密値への影響はない。
- **修正状況**：既知のWindows Wrangler実行環境差として記録し、Linux CIまたはCloudflare実行環境のdry-runを合格証拠に使う。ローカルの型検査・lint・web testは別途合格している。
- **再発防止**：Windowsローカルの終了コードだけでWorker buildを合格扱いにせず、CIの実行結果とbinding解決ログを併用する。

## 2026-08-29 個人PWA方針のGoalスキーマ反映漏れ

- **何が起きたか**：`shared/kyozai-parity-goal.json`へ個人PWA方針の`deliveryMode`／`saasFrozen`を追加した際、厳格なGoal JSON Schemaへ同じプロパティを追加し忘れ、CIの`validate:goal`が拒否した。
- **影響**：アプリの型検査・テスト・E2Eには影響せず、CIの契約検証ジョブと`ci-green`だけが失敗した。
- **修正**：Goal Schemaへ両プロパティの型と許容値を追加し、`pnpm validate:goal`、skill検証、Webテスト178件、型検査、lintを再実行して合格させた。
- **再発防止**：Goal JSONの方針フィールドを変更する際は、JSON本体と厳格Schemaを同一コミットで更新し、CI契約検証を先に実行する。

## 2026-08-29 Google SDK追加時のpnpm build-script policy未登録

- **何が起きたか**：Geminiの公式SDKを追加した直後、`pnpm`の依存同期がSDKとその依存`protobufjs`のbuild scriptを未登録として停止した。続いてpnpmが案内用の重複YAML行を追記し、YAML解析も停止した。
- **影響**：ローカル型検査が一時停止しただけで、Production、秘密値、Provider呼出しには影響しなかった。
- **修正**：`pnpm-workspace.yaml`の`allowBuilds`へ両依存を明示的に`false`で登録し、案内用の重複行を削除した。SDKはbuild scriptを実行せずに同期・型検査できることを確認する。
- **再発防止**：新しい依存を追加する際は、先にbuild-script policyを確認し、実行不要なscriptを暗黙に許可しない。

## 2026-08-29 Workspaceの画像再開実装が行数lintを超過

- **何が起きたか**：画像checkpointと再開UIを既存`workspace.tsx`へ加えた結果、max-lines lintの上限を超えた。
- **影響**：lintのみが停止し、画像生成の実行や保存済みデータには影響しなかった。
- **修正**：入力画面を`workspace-input-view.tsx`へ分離し、Workspaceには状態遷移と呼び出しだけを残した。
- **再発防止**：画面状態と表示部品を同一ファイルへ積み増さず、表示部品は独立した単位で分ける。

## 2026-08-29 Windowsでcontrol-plane dry-runが再度終了コード3221226505

- **何が起きたか**：個人PWA画像修正の全体buildで、変更していない`apps/control-plane`の`wrangler deploy --dry-run`がWindows固有の終了コード`3221226505`で停止した。
- **影響**：Webの画像処理buildは単独で完走しており、今回のWebコード、Provider、秘密値には影響しない。
- **対応**：Web build、smoke、E2Eを対象範囲の証拠として個別に完走した。control-planeの合格証拠は既存方針どおりLinux CIまたはCloudflare実行環境を使う。
- **再発防止**：PWAのみの変更でWindows control-plane終了コードを画像機能の不合格へ混同しない。一方でCIではcontrol-plane jobを省略しない。

## 2026-08-29 個人PWA画像canaryでGemini Provider 429と教材生成の一時502を観測

- **何が起きたか**：画像修正後のProduction canaryで、Gemini画像ProviderはGoogle側429を返した。アプリは`SERVICE_UNAVAILABLE`と`image_provider_response`で安全に返却した。また、最小教材生成を複数回行った後の1回が一般的な上流502となった。
- **影響**：Gemini選択時はProviderが利用可能になるまで画像生成できない。OpenAI `gpt-image-2-medium`は同じProductionで実画像生成・画像QA・PNG hash検証まで成功した。教材生成の一時502は今回の画像render adapterの障害と断定しない。
- **対応**：GeminiのBeta wire契約を公式Models API SDKへ置換済みで、Provider／decode／normalize／QAの失敗段階とrequest IDを表示・ログ相関可能にした。成功済み画像は端末内checkpointから失敗ページだけ再開する。
- **再発防止**：通常CIはrecorded fixtureでProvider契約を検査し、実Providerは低コスト1枚canaryに分離する。Provider 429をモデル契約エラーやSharp障害として混同しない。

## 2026-08-29 ローカルE2Eの直後再実行が一時的にポートを使用中と判定した

- **何が起きたか**：前回のPlaywright実行後、OSの`TIME_WAIT`中にE2Eを直後再実行したため、`localhost:3125`を使用中としてPlaywrightが二重起動を拒否した。
- **影響**：ローカルの重複したE2E実行1回だけが停止した。アプリ、Production、Provider呼出し、秘密値には影響しない。
- **修正**：listenerの所有プロセスがないことを確認して解放を待ち、同じE2Eを1回だけ再実行して3件すべて成功した。
- **再発防止**：E2E直後の再実行前にポート解放を確認し、使用中ならPlaywrightを重ねて起動しない。

## 2026-08-29 Vercel配備でworkspaceの実行位置を二重に指定した

- **何が起きたか**：`pnpm --filter web exec vercel`をroot directoryが`apps/web`のVercel projectへ実行し、CLIが`apps/web/apps/web`を参照して配備前に拒否した。
- **影響**：ファイル送信・build・Production切替の前に停止したため、Production、Provider、秘密値に影響はない。
- **修正**：rootにlinkされたVercel projectから`vercel --prod --yes`を実行する。
- **再発防止**：Vercel projectのRoot DirectoryとCLIの現在ディレクトリを配備前に照合し、filter execと`--cwd`を重ねない。

## 2026-08-29 個人PWAの教材本文生成が504で未完了になった

- **何が起きたか**：Productionの`POST /api/generate`がHTTP 504で終了した。ブラウザーとVercel request logで確認し、画像生成前の教材本文生成が未完了だった。
- **影響**：その1回の教材生成は成果物を返せなかった。画像生成のOpenAI canary成功、既存の画像品質処理、秘密値には影響しない。
- **対応**：未特定のProvider停止原因を推測してtimeout・再試行・品質工程を変更せず、本文生成の開始工程、経過時間、相関request IDだけを安全に記録する。
- **再発防止**：504を受けたら工程別の安全な実行記録で停止箇所を特定してから、その工程だけを修正する。

## 2026-08-29 Windowsの一時ファイルロックでWeb buildが1回停止した

- **何が起きたか**：`next build`が`.next/server/app/manifest.webmanifest`の削除時にWindowsの`EPERM`で停止した。Next／Playwrightの所有プロセスは確認時に存在しなかった。
- **影響**：ローカルbuild 1回だけが停止した。ソース、Production、Provider、秘密値には影響しない。
- **修正**：所有プロセスを終了せず解放を待って同じbuildを再実行し、正常に完走した。
- **再発防止**：Windowsの生成物ロックではプロセス名だけで終了させず、所有者を確認して解放待ち後に1回だけ再実行する。

## 2026-08-29 Vercel project照会で環境変数の暗号化済み表現をツール出力へ含めた

- **何が起きたか**：504の実行設定を確認するためVercel project APIを広く照会し、環境変数一覧に含まれる暗号化済み表現がツール出力へ現れた。平文のAPIキー・トークン・接続文字列は表示されていない。
- **影響**：運用中の秘密設定に由来する暗号化済み表現を出力経路へ露出させた。平文を復元できるかはこの記録から判定しない。登録済みの運用秘密として扱い、Vercel上で利用者が直接再生成・差し替えを行う必要がある。
- **対応**：出力値を利用・再掲しない。利用者へ値を会話へ送らずVercelの環境変数画面で直接差し替えるよう依頼する。
- **再発防止**：環境変数を含みうるproject全体取得を使わず、必要な設定フィールドだけを返すAPIまたは画面を利用する。秘密値・暗号化表現を含む出力は診断に使わない。

## 2026-08-29 本文生成SSEの本文読取がtimeoutで中断されなかった

- **何が起きたか**：Productionの教材本文生成で`analysis`工程の開始だけが記録され、OpenAIのSSE本文読取がアプリのdeadline内に終わらず、アプリのcatchより外側で504となった。
- **影響**：その教材生成は画像生成前に停止した。本文・画像の品質基準や成功済み画像生成には影響しない。
- **修正**：SSE本文の`reader.read()`へ、既存Provider試行の残り時間を使う明示的なtimeoutとreaderの終了処理を追加する。
- **再発防止**：HTTPヘッダー取得だけで外部呼出しのtimeoutを完了扱いにせず、stream本文読取にも同じdeadlineを適用する。
