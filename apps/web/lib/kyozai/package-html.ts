import { DESIGN_PROFILE, formatDuration, layoutClass, slideDurationSeconds, splitItems } from "./design";
import type { Slide, TeachingPackage } from "./types";

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[character] || character));
}

function list(items: string[], className = "bullets"): string {
  return `<ul class="${className}">${items.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>`;
}

function slideBody(slide: Slide): string {
  if (slide.layoutFamily === "cover") return `<div class="cover-body"><p class="key-message">${escapeHtml(slide.keyMessage)}</p><div class="cover-points">${slide.bullets.map((item, index) => `<span><b>${String(index + 1).padStart(2, "0")}</b>${escapeHtml(item)}</span>`).join("")}</div></div>`;
  if (slide.layoutFamily === "compare") {
    const [left, right] = splitItems(slide.bullets);
    return `<div class="compare"><section><span>${escapeHtml(slide.labels[0] || "")}</span>${list(left)}</section><section><span>${escapeHtml(slide.labels[1] || "")}</span>${list(right)}</section></div>`;
  }
  if (slide.layoutFamily === "sequence") return `<ol class="sequence">${slide.bullets.map((item, index) => `<li><b>${index + 1}</b><span>${escapeHtml(item)}</span></li>`).join("")}</ol>`;
  if (slide.layoutFamily === "evidence") return `<div class="evidence"><strong>${escapeHtml(slide.keyMessage)}</strong>${list(slide.bullets)}</div>`;
  if (slide.layoutFamily === "checklist") return `<div class="checklist">${slide.bullets.map((item) => `<p><span>✓</span>${escapeHtml(item)}</p>`).join("")}</div>`;
  if (slide.layoutFamily === "action") return `<div class="action"><p>${escapeHtml(slide.keyMessage)}</p><ol>${slide.bullets.map((item, index) => `<li><b>${index + 1}</b>${escapeHtml(item)}</li>`).join("")}</ol></div>`;
  return `<div class="focus"><strong>${escapeHtml(slide.keyMessage)}</strong>${list(slide.bullets)}</div>`;
}

function slideMarkup(slide: Slide, total: number): string {
  const seconds = slideDurationSeconds(slide);
  return `<section class="slide-page"><div class="slide ${layoutClass(slide.layoutFamily)}"><div class="slide-header"><span></span><span>${String(slide.number).padStart(2, "0")} / ${String(total).padStart(2, "0")}</span></div><h2>${escapeHtml(slide.title)}</h2><div class="title-rule"></div>${slideBody(slide)}</div><aside class="notes"><div><strong>講師ノート</strong><span>${slide.speakerNotes.length}字 / ${formatDuration(seconds)}</span></div><p>${escapeHtml(slide.speakerNotes)}</p></aside></section>`;
}

const { palette, typography } = DESIGN_PROFILE;

const styles = `
*{box-sizing:border-box}body{margin:0;background:#e9edf3;color:${palette.text};font-family:${typography.family};letter-spacing:0;line-height:1.65}.package-header,.chapter{max-width:1100px;margin:30px auto;background:white;padding:40px}.package-header{border-top:8px solid ${palette.primary}}.package-header h1{font-size:36px;line-height:1.3;margin:10px 0}.meta{color:${palette.muted}}.slide-page{max-width:1180px;margin:28px auto}.slide{position:relative;aspect-ratio:16/9;background:${palette.background};color:${palette.text};padding:5.5% 6%;overflow:hidden;box-shadow:0 12px 30px rgba(10,20,38,.16)}.slide-header,.slide-footer{position:absolute;left:6%;right:6%;display:flex;justify-content:space-between;color:${palette.muted};font-size:12px;font-weight:700}.slide-header{top:4%}.slide-footer{bottom:3.5%}.slide h2{font-size:clamp(30px,3.35vw,54px);line-height:1.22;margin:2.5% 0 1.5%;max-width:94%}.title-rule{width:100%;height:4px;background:${palette.primary};margin-bottom:4%}.key-message,.focus>strong,.evidence>strong,.action>p{font-size:clamp(22px,2.35vw,38px);line-height:1.45;font-weight:800}.bullets{margin:0;padding-left:1.25em;font-size:clamp(18px,1.55vw,26px)}.bullets li{margin:.55em 0}.cover-body{margin-top:7%;max-width:80%}.cover-points{display:flex;gap:16px;margin-top:5%}.cover-points span{display:flex;align-items:center;gap:12px;background:${palette.secondaryBackground};border-left:5px solid ${palette.primary};padding:16px 20px;font-size:20px;font-weight:700}.cover-points b{color:${palette.primary}}.compare{display:grid;grid-template-columns:1fr 1fr;gap:4%}.compare section{border-top:6px solid ${palette.primary};background:${palette.secondaryBackground};padding:5%;min-height:230px}.compare section>span{display:inline-grid;place-items:center;min-width:96px;height:40px;padding:0 12px;background:${palette.primary};color:white;font-size:14px;font-weight:900;margin-bottom:18px}.sequence{list-style:none;padding:0;margin:4% 0 0;display:flex;gap:2%}.sequence li{position:relative;flex:1;min-height:180px;background:${palette.secondaryBackground};padding:54px 18px 18px;font-size:20px;font-weight:700}.sequence li:not(:last-child):after{content:'›';position:absolute;right:-8%;top:43%;font-size:38px;color:${palette.primary}}.sequence b{position:absolute;top:0;left:0;width:42px;height:42px;display:grid;place-items:center;background:${palette.primary};color:white}.evidence{display:grid;grid-template-columns:1.05fr .95fr;gap:6%;align-items:center}.evidence>strong{color:${palette.primary};font-size:clamp(30px,3.4vw,56px)}.checklist{display:grid;grid-template-columns:1fr 1fr;gap:16px}.checklist p{margin:0;background:${palette.secondaryBackground};padding:18px;font-size:20px;font-weight:700;display:flex;gap:13px;align-items:center}.checklist span{display:grid;place-items:center;width:34px;height:34px;border-radius:50%;background:${palette.primary};color:white}.action{display:grid;grid-template-columns:.9fr 1.1fr;gap:6%;align-items:center}.action>p{color:${palette.primary}}.action ol{list-style:none;padding:0;margin:0}.action li{display:flex;align-items:center;gap:16px;border-bottom:1px solid ${palette.border};padding:14px 0;font-size:20px;font-weight:700}.action li b{display:grid;place-items:center;width:38px;height:38px;background:${palette.primary};color:white}.focus{display:grid;grid-template-columns:1.05fr .95fr;gap:7%;align-items:center}.focus>strong{color:${palette.primary}}.notes{background:white;border:1px solid #cfd5df;padding:22px 26px}.notes>div{display:flex;justify-content:space-between;color:${palette.primaryDark}}.notes p{margin-bottom:0}.chapter article{border-bottom:1px solid #d8dde5;padding:15px 0}.chapter h2{border-bottom:4px solid ${palette.primary};padding-bottom:10px}@media print{body{background:white}.package-header,.chapter{margin:0;max-width:none}.slide-page{margin:0;page-break-after:always;max-width:none}.slide{box-shadow:none;width:100%}.notes{border:0;border-top:1px solid #bbb}.chapter{page-break-before:always}}@media(max-width:700px){.slide-page{margin:12px}.slide{padding:6%}.slide h2{font-size:22px}.slide-header,.slide-footer{font-size:7px}.key-message,.focus>strong,.evidence>strong,.action>p{font-size:15px}.bullets,.compare .bullets{font-size:11px}.cover-points span,.sequence li,.checklist p,.action li{font-size:10px}.notes{padding:16px}.package-header,.chapter{margin:12px;padding:22px}}
`;

export function packageHtml(result: TeachingPackage): string {
  const objectives = result.learningObjectives.map((item) => `<li>${escapeHtml(item)}</li>`).join("");
  const slides = result.slides.map((slide) => slideMarkup(slide, result.slides.length)).join("");
  const scenario = result.scenario.map((item) => `<article><h3>${escapeHtml(item.section)} <small>${item.minutes}分</small></h3><p>${escapeHtml(item.guidance)}</p></article>`).join("");
  const faq = result.faq.map((item) => `<article><h3>Q. ${escapeHtml(item.question)}</h3><p>A. ${escapeHtml(item.answer)}</p></article>`).join("");
  const quiz = result.quiz.map((item, index) => `<article><h3>${index + 1}. ${escapeHtml(item.question)}</h3><ol type="A">${item.options.map((option) => `<li>${escapeHtml(option)}</li>`).join("")}</ol><p><strong>正解:</strong> ${escapeHtml(item.options[item.answerIndex] || "")} / ${escapeHtml(item.explanation)}</p></article>`).join("");
  return `<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${escapeHtml(result.title)} | KYOZAI</title><style>${styles}</style><style>@page{size:A4 landscape;margin:8mm}@media print{.slide-page{break-inside:avoid}.notes{break-inside:avoid}}</style></head><body><header class="package-header"><small>${escapeHtml(result.designProfile)}</small><h1>${escapeHtml(result.title)}</h1><p class="meta">対象: ${escapeHtml(result.targetAudience)} / 想定時間: ${result.durationMinutes}分</p><p>${escapeHtml(result.sourceSummary)}</p><h2>学習目標</h2><ul>${objectives}</ul></header>${slides}<section class="chapter"><h2>講師シナリオ</h2>${scenario}</section><section class="chapter"><h2>FAQ</h2>${faq}</section><section class="chapter"><h2>確認テスト</h2>${quiz}</section></body></html>`;
}
