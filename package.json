{
  "name": "codex-slide-maker",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "audit": "pnpm audit --audit-level moderate",
    "validate:skills": "node scripts/validate-skills.mjs",
    "validate:goal": "node scripts/validate-goal-contract.mjs",
    "validate:process": "node scripts/validate-process-contract.mjs",
    "validate:blind-parity": "node scripts/validate-blind-parity.test.mjs",
    "validate:blind-semantic": "node scripts/validate-blind-semantic-evidence.test.mjs",
    "validate:real-package-evidence": "node scripts/validate-real-package-evidence.test.mjs",
    "validate:revise": "node scripts/validate-revision-benchmark.mjs && node --test .agents/skills/kyozai-revise/scripts/validate_revision.test.mjs"
  },
  "devDependencies": {
    "ajv": "8.18.0",
    "typescript": "^5.7.3"
  }
}
