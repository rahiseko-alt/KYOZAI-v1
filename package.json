{
  "name": "codex-slide-maker",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "audit": "pnpm audit --audit-level moderate",
    "validate:skills": "node scripts/validate-skills.mjs",
    "validate:process": "node scripts/validate-process-contract.mjs",
    "validate:revise": "node scripts/validate-revision-benchmark.mjs && node scripts/validate-revision-phase1.mjs && node --test .agents/skills/kyozai-revise/scripts/validate_revision.test.mjs",
    "test:revise:forward": "pnpm --dir apps/web exec node --env-file-if-exists=.env.local --import tsx ../../scripts/forward-test-revision.ts"
  },
  "devDependencies": {
    "ajv": "8.18.0",
    "typescript": "^5.7.3"
  }
}
